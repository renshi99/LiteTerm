# LiteTerm V0.5 测试计划

## 1. 目的与范围

本计划用于验证 LiteTerm V0.5“日志快捷能力”，重点覆盖：

- 常用命令的新增、编辑、删除、校验和 SQLite 持久化。
- `{path}` 模板变量展开、POSIX Shell 参数转义和执行前预览。
- 命令只发送到预期的已连接 SSH 标签，失败时不崩溃、不串会话。
- 一键 `tail -n 500 -F` 和进入日志目录。
- 按服务器保存常用日志路径，以及后续一键 SFTP 下载日志。
- 深浅主题、不同 DPI、键盘操作和敏感信息保护。

本计划包含当前已实现功能的立即验收用例，以及 V0.5 后续功能的预留用例。预留用例只有在对应功能实现并通过构建后才执行，不应在功能尚未实现时记为失败或阻塞。

## 2. 功能就绪状态

| 能力 | 当前状态 | 对应用例 |
|---|---|---|
| 常用命令新增、编辑、删除 | 已实现，待真实界面验收 | CMD-01～CMD-05 |
| 默认命令与 SQLite 持久化 | 已实现，自动测试通过，待重启验收 | CMD-01、CMD-04、PST-01 |
| `{path}` 展开及 POSIX 转义 | 已实现，自动测试通过，待真实服务器验收 | TPL-01～TPL-06 |
| 执行预览和明确执行 | 已实现，待真实界面验收 | EXE-01～EXE-04、SEC-01 |
| 一键持续追踪、进入日志目录 | 已以内置模板实现，待真实终端验收 | EXE-01、EXE-02 |
| 按服务器保存常用日志路径 | 已实现，自动测试通过，待真实界面验收 | LOG-01～LOG-04 |
| 一键 SFTP 下载日志 | 尚未实现 | DNL-01～DNL-04 |
| 日志关键词基础高亮 | 可选增强，尚未实现 | OPT-01 |

## 3. 自动验证门禁

开始人工测试前必须执行：

```powershell
dotnet build LiteTerm.sln -c Release --no-restore
dotnet test LiteTerm.sln -c Release --no-build
git diff --check
```

当前基线：

- Release 构建和 WPF/XAML 编译：0 警告、0 错误。
- 常规回归：113 项通过，9 项外部 SSH/SFTP 条件测试按配置跳过。
- 已覆盖路径空格、中文兼容基础、单引号、Shell 元字符、前后空格保留、换行及控制字符拒绝。
- 已覆盖未知模板变量、大小写不敏感的重复名称、默认配置、空配置和 SQLite 跨实例恢复。

若代码基线变化，执行人员应以本轮实际结果更新以上数字。任何构建失败或非外部条件测试失败都必须先修复，不进入人工验收。

## 4. 测试版本与环境

- 可执行文件：`src\LiteTerm.App\bin\Release\net10.0-windows\LiteTerm.App.exe`
- 操作系统：Windows 10/11 x64
- 运行时：.NET 10 Windows Desktop Runtime、WebView2 Runtime
- 测试服务器：允许 SSH/SFTP 的专用 Linux 服务器
- 测试账号：只拥有专用测试目录权限的非生产账号
- 显示环境：至少当前 DPI；发布验收时补测 100% 和 150%

执行前记录：

| 项目 | 实际值 |
|---|---|
| 测试日期 | 待填写 |
| Git 提交/工作区标识 | 待填写 |
| Windows 版本 | 待填写 |
| DPI/缩放比例 | 待填写 |
| LiteTerm 构建时间 | 待填写 |
| 测试服务器系统 | 待填写，不记录地址和凭据 |
| 测试人员 | 待填写 |

## 5. 测试数据与安全要求

1. 使用唯一标识，例如 `LITETERM-V05-20260829-01`。
2. 远程文件只放在 `$HOME/liteterm-v05-<唯一标识>`；破坏性测试前使用 `pwd` 和 `realpath` 再次确认路径。
3. 本地下载只放在 `C:\Temp\LiteTerm-V05-<唯一标识>`。
4. 命令模板、远程路径、截图和缺陷记录中不得包含密码、私钥、令牌或生产日志内容。
5. 测试命令中的路径应使用 `realpath` 得到的绝对路径。当前 `{path}` 会被编码为字面量参数，不会展开 `$HOME` 或 `~`。
6. 测试 Shell 元字符时必须先创建同名的无害测试文件，并使用哨兵文件确认没有发生额外命令执行。
7. 除持久化专项外，测试结束后恢复原常用命令配置；不确定原配置时先截图或人工抄录名称和模板，禁止记录凭据。

建议在测试服务器准备以下数据：

```bash
test_root="$HOME/liteterm-v05-LITETERM-V05-20260829-01"
mkdir -p -- "$test_root/含 空格目录"
printf 'line-1\n中文日志\nERROR sample\n' > "$test_root/application.log"
printf 'space path\n' > "$test_root/含 空格目录/应用 日志.log"
printf 'special path\n' > "$test_root/app's;\$(echo INJECTED).log"
realpath -- "$test_root"
```

把 `realpath` 输出记为 `<TEST_ROOT>`，后续界面输入均使用该绝对路径。不要在生产主机或生产目录执行上述命令。

## 6. 优先级与结果标记

- `P0`：发布门禁；失败时不得认为 V0.5 可验收。
- `P1`：主要体验和兼容性；不得存在崩溃、误执行、数据损坏或会话串扰。
- `P2`：可选增强或非主路径。

结果标记：

- `通过`：实际结果完全符合预期。
- `失败`：行为错误、崩溃、卡死、误执行、数据损坏或资源泄漏。
- `阻塞`：功能已就绪，但环境、权限或工具限制导致无法执行；必须记录原因。
- `待实现`：对应功能尚未完成，不计入当前通过率。
- `跳过`：功能已就绪但经批准本轮不执行；必须记录原因。

缺陷至少记录：用例编号、构建标识、复现步骤、输入模板、脱敏后的路径、预览文本、实际结果、预期结果、截图、发生时间和复现概率。不得记录真实凭据。

## 7. 测试用例

### A. 启动与入口状态

#### SMK-01 启动与快捷命令入口（P0）

步骤：

1. 启动 Release 版 LiteTerm，等待终端初始化。
2. 在未连接状态观察“快捷命令”按钮。
3. 连接专用测试服务器，切换到已连接标签。
4. 断开连接并再次观察按钮。

预期：

- 应用正常启动，无崩溃和未处理异常。
- 未连接时按钮禁用；连接成功后启用；断开或连接失败后再次禁用。
- 按钮状态始终跟随当前标签，不受后台标签状态污染。

#### SMK-02 默认配置首次加载（P0）

准备：使用尚未保存过 `quick.commands` 的隔离数据库或全新测试用户数据目录。

步骤：连接服务器并打开“快捷命令”。

预期：列表包含“持续追踪日志（末尾 500 行）”和“进入日志目录”；模板分别为 `tail -n 500 -F -- {path}` 和 `cd -- "$(dirname -- {path})"`；窗口布局完整，无截断。

### B. 常用命令配置

#### CMD-01 新增和编辑命令（P0）

步骤：

1. 新增命令，名称填写 `V05 查看字节数`，模板填写 `wc -c -- {path}`。
2. 点击“应用当前编辑”，切换到其他命令再切回。
3. 将名称修改为 `V05 查看日志字节数` 并保存配置。
4. 重新打开窗口。

预期：新增、切换和编辑过程不丢失已应用内容；重新打开后只存在修改后的名称和模板。

#### CMD-02 删除命令（P0）

步骤：删除 `V05 查看日志字节数`，保存并重新打开窗口。

预期：目标命令不再出现；其他命令顺序和内容不变；删除不触发任何终端命令。

#### CMD-03 名称与模板校验（P0）

分别尝试：

- 空名称或仅空格名称。
- 空模板。
- 与现有名称仅大小写不同的名称。
- 未支持变量 `cat -- {file}`。
- 粘贴包含换行或控制字符的模板。

预期：均显示明确校验信息，窗口不崩溃，不保存无效记录，不向终端发送内容；`${HOME}` 可作为普通 Shell 环境变量文本保存，不被误判为 LiteTerm 模板变量。

#### CMD-04 空配置持久化（P1）

步骤：在已备份原配置的隔离环境删除全部命令，保存，关闭并重启 LiteTerm。

预期：重启后仍为空列表，不因配置为空而重新注入默认命令；可再次新增命令并正常保存。

#### CMD-05 数量及长度边界（P1）

步骤：验证第 50 条命令可保存，第 51 条被拒绝；验证 80 字符名称可保存、81 字符被拒绝；验证超长模板被拒绝。

预期：边界内成功，越界时给出明确提示；已有配置不丢失。允许使用自动化测试生成边界数据，人工界面只需抽查错误提示。

### C. 模板预览与路径转义

#### TPL-01 普通绝对路径（P0）

模板：`wc -c -- {path}`

路径：`<TEST_ROOT>/application.log`

预期：预览为 `wc -c -- '<TEST_ROOT>/application.log'`，执行结果与远程 `wc -c` 一致。

#### TPL-02 中文与空格路径（P0）

模板：`wc -c -- {path}`

路径：`<TEST_ROOT>/含 空格目录/应用 日志.log`

预期：完整路径被一对 Shell 单引号包围，中文和空格保持不变；执行成功且只访问一个文件参数。

#### TPL-03 单引号路径（P0）

模板：`wc -c -- {path}`

路径：`<TEST_ROOT>/app's;$(echo INJECTED).log`

预期：预览中的单引号编码为 `'"'"'` 连接形式；路径其余字符保持在受保护参数中；命令可读取准备好的同名文件。

#### TPL-04 Shell 元字符不注入（P0）

步骤：

1. 确认 `<TEST_ROOT>/SHOULD_NOT_EXIST` 不存在。
2. 模板使用 `wc -c -- {path}`，路径输入 `<TEST_ROOT>/missing.log; touch <TEST_ROOT>/SHOULD_NOT_EXIST; #`，核对整个输入在预览中是一个单引号参数后执行。
3. 在远端执行 `test ! -e '<TEST_ROOT>/SHOULD_NOT_EXIST'`。

预期：`wc` 只报告该字面量路径不存在；分号、空格和井号没有被 Shell 解释，没有执行 `touch`，没有创建哨兵文件或其他意外文件。TPL-03 同时证明 `$()` 在实际存在的文件名中保持为字面量。

#### TPL-05 缺少路径（P0）

步骤：选择包含 `{path}` 的模板，清空路径，观察预览并点击执行。

预期：显示“需要填写远程路径”类提示；无法执行；终端中没有出现空路径命令或额外换行。

#### TPL-06 不含路径变量的模板（P1）

模板：`printf 'LITETERM-V05-PLAIN\n'`

路径：留空

预期：预览与模板一致，可正常执行；不会强制要求路径。

### D. 命令执行与标签隔离

#### EXE-01 一键持续追踪日志（P0）

步骤：

1. 选择内置“持续追踪日志（末尾 500 行）”。
2. 输入 `<TEST_ROOT>/application.log`，核对预览后点击“执行所选”。
3. 在另一 SSH 会话向文件追加唯一文本。
4. 在 LiteTerm 中按 `Ctrl+C`。

预期：显示已有末尾内容并实时显示追加行；`Ctrl+C` 能停止 `tail -F`；提示符恢复且会话继续可用。

#### EXE-02 一键进入日志目录（P0）

步骤：选择“进入日志目录”，路径填写 `<TEST_ROOT>/application.log`，执行后在终端手工输入 `pwd`。

预期：预览使用安全转义；`pwd` 输出为 `<TEST_ROOT>`；路径中含空格或单引号时同样进入正确父目录。

#### EXE-03 保存不执行（P0）

步骤：编辑一个模板，点击“保存配置”而不是“执行所选”，随后观察终端。

预期：配置被保存，但终端没有收到模板内容、换行或任何命令。

#### EXE-04 两标签发送隔离（P0）

步骤：连接两个标签；分别在各标签打开快捷命令并执行包含不同唯一标识的 `printf` 命令。

预期：每条输出只出现在发起操作的标签；标题、状态和终端内容不串用；一个标签断开不影响另一个标签继续执行快捷命令。

### E. 持久化、错误与安全

#### PST-01 重启恢复（P0）

步骤：新增一条带唯一名称的有效命令，保存，完全退出 LiteTerm，确认无残留进程后重启。

预期：名称和模板完整恢复；顺序保持；无需连接服务器即可完成数据库初始化，连接后可打开并执行。

#### ERR-01 远端命令失败（P0）

步骤：执行 `wc -c -- {path}`，路径指向专用目录内不存在的文件。

预期：错误由远端 Shell 显示在当前终端；LiteTerm 不崩溃，SSH 连接保持可用，可继续执行后续命令。

#### ERR-02 执行前或发送中连接断开（P1）

步骤：在专用网络环境中使当前 SSH 会话断开，再尝试打开或执行快捷命令。

预期：入口被禁用或出现明确提示；不会把命令发送到其他标签；应用不崩溃。若模态窗口使该时序无法稳定人工制造，可由会话替身自动测试覆盖并记录原因。

#### SEC-01 预览不等于执行（P0）

步骤：编辑模板和路径，多次触发预览、切换列表，最后取消窗口。

预期：预览过程、列表切换和取消均不发送任何终端输入；只有点击“执行所选”才执行。

#### SEC-02 敏感信息与持久化检查（P0）

步骤：检查快捷命令错误提示、截图、SQLite `app_setting` 和测试记录。

预期：不包含密码、私钥、口令或令牌。命令模板和日志路径按产品功能以普通设置保存，因此测试数据必须是非敏感专用路径；不得把凭据嵌入模板。

### F. UI、主题与可访问性

#### UI-01 深浅主题布局（P1）

步骤：分别在深色和浅色主题打开快捷命令窗口，调整至最小尺寸和较大尺寸。

预期：列表、编辑区、预览区和底部按钮均可见；文字对比度正常；没有重叠、截断或无法滚动的内容。

#### UI-02 DPI 与键盘操作（P1）

步骤：在 100% 和 150% DPI 各打开一次窗口；使用 `Tab`、`Shift+Tab`、方向键、`Enter` 和 `Esc`。

预期：焦点顺序合理；列表可用键盘选择；`Esc` 取消且不执行；执行前焦点和默认按钮行为不会导致误执行。

### G. 分批启用功能

LOG 用例对应功能已实现并转为正式验收门禁；DNL 用例仍待一键下载功能实现后启用。

#### LOG-01 按服务器保存日志入口（P0）

预期：日志名称、绝对远程路径和所属服务器资料原子保存；切换服务器只显示对应入口；复制服务器资料不复制日志入口，删除资料会级联删除日志入口并立即禁用相关已连接标签的入口按钮。

#### LOG-02 日志入口 CRUD 和重启恢复（P0）

预期：新增、编辑、删除、重复名称、非法路径和空值处理明确；重启后恢复且不影响其他服务器。

#### LOG-03 少量操作打开目标日志（P0）

预期：从服务器列表到连接并打开目标日志不超过产品定义的少量操作；连接失败、日志不存在或权限不足时提示明确。

#### LOG-04 复用命令模板（P0）

预期：日志入口的一键追踪和进入目录复用统一模板渲染器，不重复拼接未经转义的 Shell 命令；执行前仍显示最终预览。

#### DNL-01 一键下载小日志（P0）

预期：使用独立 SFTP 会话流式下载到用户选择的位置；内容一致；终端交互不受影响。

#### DNL-02 本地冲突与覆盖（P0）

预期：目标已存在时明确询问；拒绝后原文件不变；允许后通过临时文件完成替换，不留下截断目标。

#### DNL-03 大日志进度、取消和释放（P0）

预期：显示字节数、百分比和速度；取消后清理临时文件且既有目标不损坏；关闭标签或主窗口会取消并等待独立 SFTP 会话释放。

#### DNL-04 远端错误类型（P1）

预期：目录、符号链接、权限不足、文件消失和连接中断均有明确错误；不会整体读入内存，不会影响 SSH 终端会话。

#### OPT-01 日志关键词基础高亮（P2，可选）

预期：仅改变显示，不修改远端输出、不破坏 ANSI 控制序列、选择复制和终端性能；关闭增强后恢复原始显示。

### H. 清理

#### CLN-01 恢复配置与删除测试数据（P0）

步骤：

1. 删除带 `V05` 或唯一标识的测试命令，恢复测试前配置。
2. 确认远程路径后删除专用测试目录。
3. 删除本地下载目录。
4. 重启 LiteTerm，确认没有测试配置残留。

预期：只清理本轮专用数据；原服务器资料、凭据、已知主机、终端外观和其他常用命令不受影响。

## 8. 测试结果汇总

| 用例 | 优先级 | 初始状态 | 实际结果/缺陷编号 |
|---|---:|---|---|
| SMK-01 | P0 | 通过 | Release 启动正常；未连接时快捷命令禁用，真实 SSH 连接后启用，主动断开后再次禁用；关闭后无残留进程。 |
| SMK-02 | P0 | 通过 | 首次打开显示两条默认命令，名称和模板与计划一致；窗口完整显示列表、编辑、预览及底部按钮。 |
| CMD-01 | P0 | 阻塞 | 模态窗口可打开并聚焦字段，但 Computer Use 的文字输入未进入控件，无法安全执行新增和编辑。 |
| CMD-02 | P0 | 阻塞 | 未创建临时命令；没有删除正式配置。删除操作同时需要单独的破坏性 UI 确认。 |
| CMD-03 | P0 | 阻塞 | 模型校验和自动测试通过；模态文字输入路由失败，未执行界面错误提示验收。 |
| CMD-04 | P1 | 阻塞 | 空配置 SQLite 自动测试通过；为避免修改正式配置且无法完成恢复，本轮未执行界面清空。 |
| CMD-05 | P1 | 阻塞 | 数量和长度限制由核心模型实现；模态文字输入路由失败，未执行界面边界输入。 |
| TPL-01 | P0 | 阻塞 | 普通路径渲染已有单元测试；无法向模态路径框输入文本完成界面预览。 |
| TPL-02 | P0 | 阻塞 | 空格路径的单参数转义已有单元测试；中文和空格界面输入未能路由。 |
| TPL-03 | P0 | 阻塞 | 单引号编码已有单元测试；界面特殊路径输入未能路由。 |
| TPL-04 | P0 | 阻塞 | Shell 元字符受单引号保护已有单元测试；禁止自动向终端注入命令，未执行真实无注入探针。 |
| TPL-05 | P0 | 通过 | 默认含 `{path}` 命令在路径为空时显示“填写远程路径后将显示完整预览”，执行预览为空；模型拒绝缺少路径已有测试。 |
| TPL-06 | P1 | 阻塞 | 不含路径变量模板已有单元测试；无法在模态窗口输入自定义模板。 |
| EXE-01 | P0 | 阻塞 | 默认 `tail -n 500 -F` 模板显示正确；Computer Use 安全规则禁止自动向终端应用输入命令。 |
| EXE-02 | P0 | 阻塞 | 默认进入日志目录模板显示正确；路径输入和终端 `pwd` 均需人工键盘操作。 |
| EXE-03 | P0 | 阻塞 | 列表切换和取消窗口没有向终端发送内容；因不能编辑文本，未覆盖“编辑后保存不执行”的完整步骤。 |
| EXE-04 | P0 | 阻塞 | 标签隔离已有 V0.4 真实会话证据；本轮无法从快捷窗口输入两条唯一命令完成专项验证。 |
| PST-01 | P0 | 阻塞 | 默认配置跨进程加载正常；无法创建临时命令并保存，因此未完成自定义配置重启恢复。 |
| ERR-01 | P0 | 阻塞 | 远端错误保持会话可用已有 SSH 测试基础；无法输入不存在的路径并执行快捷命令。 |
| ERR-02 | P1 | 阻塞 | 断开后入口会正确禁用；模态执行时序无法由当前自动化稳定制造。 |
| SEC-01 | P0 | 阻塞 | 打开窗口、切换内置命令和取消均未触发终端命令；无法编辑并多次触发预览，完整步骤待人工完成。 |
| SEC-02 | P0 | 通过 | 自动测试使用隔离 SQLite 验证配置持久化；本轮界面密码保持掩码，未把凭据写入模板、截图或测试记录。 |
| UI-01 | P1 | 阻塞 | 当前浅色主题下 846×593 窗口布局完整；未切换深色主题及最小尺寸复测。 |
| UI-02 | P1 | 阻塞 | 鼠标选择内置命令和取消可用；未切换 100%/150% DPI，模态键盘文字输入无法路由。 |
| LOG-01～LOG-04 | P0 | 待执行 | 功能已实现；需验证按服务器隔离、CRUD、重启恢复、安全预览和当前标签执行。 |
| DNL-01～DNL-03 | P0 | 待实现 | |
| DNL-04 | P1 | 待实现 | |
| OPT-01 | P2 | 待实现/可选 | |
| CLN-01 | P0 | 通过 | 未新增、编辑、保存或删除常用命令，未创建远程/本地测试文件；测试实例已关闭且无残留进程。 |

### 2026-08-29 执行记录

- 自动门禁：Release 构建和 WPF/XAML 编译 0 警告、0 错误；101 项测试通过，9 项外部条件测试按配置跳过；`git diff --check` 无错误。
- 真实 SSH：使用既有受保护测试资料成功连接 Ubuntu 22.04.5 LTS；连接后“快捷命令”启用，主动断开后禁用。终端欢迎信息、中文和提示符颜色清晰可见，深色终端黑底黑字修复获得正式 SSH 界面证据。
- 快捷窗口：两条默认命令及模板正确，浅色主题布局完整；列表切换正常，空路径提示明确，取消后终端保持原提示符，没有执行命令。
- 自动化限制：Computer Use 可以打开模态窗口、点击列表和聚焦远程路径框，但 `type_text` 与单字符按键均未写入该 WPF 模态控件。按安全规则不得改用 Windows 终端、PowerShell UI Automation或向终端控件注入命令，因此 CMD/TPL/EXE/PST 的输入型用例据实标记为阻塞。
- 数据与清理：没有修改正式 `quick.commands`，没有创建或删除远程、本地测试文件；LiteTerm 窗口关闭后进程已退出。

### 2026-08-29 日志入口实现记录

- 新增按已保存服务器隔离的日志入口配置，使用 SQLite 版本 4 独立表原子替换；删除服务器资料会级联清理对应入口。
- 日志窗口支持新增、编辑、删除、保存空集合、持续追踪和进入目录预览；执行前会重新确认原标签及服务器仍连接，避免切换标签误发。
- 一键追踪和进入目录复用统一 `{path}` 模板渲染器；自动测试覆盖中文、空格、单引号、分号和 `$()` 路径、每服务器 100 条边界、跨服务器隔离、迁移和级联删除。
- 自动门禁：Release 构建和 WPF/XAML 编译 0 警告、0 错误；113 项测试通过，9 项外部条件测试按配置跳过；`git diff --check` 无错误。
- 本轮未执行日志入口真实 SSH 界面输入、保存重启和最终命令发送，因此 LOG-01～LOG-04 保持“待执行”，不计为通过。

## 9. 执行批次

1. 门禁 A：自动构建、常规测试和 `git diff --check`。
2. 门禁 B：当前已实现能力的所有 P0 用例，优先执行 SMK、CMD、TPL、EXE、PST、ERR、SEC 和 CLN。
3. 回归 C：UI P1、两标签隔离、深浅主题和 DPI。
4. 功能门禁 D：按服务器日志入口实现后执行 LOG 全组。
5. 功能门禁 E：一键下载实现后执行 DNL 全组，并回归既有 SFTP 传输与关闭生命周期。

失败修复后至少重跑失败用例、同组全部 P0、SMK-01、EXE-04、SEC-01 和 CLN-01。

## 10. 通过标准

- 自动门禁构建为 0 警告、0 错误，所有非外部条件测试通过。
- 当前已实现能力的所有 P0 用例必须通过；不能用单元测试结果替代未执行的真实界面操作。
- 不得发生路径命令注入、未确认执行、跨标签误发、凭据泄露、崩溃、卡死或配置损坏。
- P1 不得存在影响主要流程、可访问性或重启恢复的高频问题。
- 后续 LOG、DNL 功能只有在对应 P0 全部通过后才可记为完成。
- 测试结束后专用远程目录、本地下载目录和测试命令均已清理，原用户数据不受影响。
