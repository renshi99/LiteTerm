# LiteTerm V0.6 错误与诊断测试计划

## 1. 目的与范围

本计划用于验证 V0.6 新增的 SSH/SFTP 统一错误模型、安全用户提示和有界诊断日志，重点覆盖：

- 连接超时、认证失败、主机身份拒绝、网络不可达和运行中断的稳定分类。
- `LastFailure` 与 `ConnectionState` 的顺序、取消语义和原标签手动重连可用性。
- SSH/SFTP 使用同一诊断日志契约，且日志故障不覆盖原始异常或阻塞释放。
- 诊断日志 UTF-8 无 BOM、1 MiB 轮转、字段固定和敏感信息脱敏。
- 主窗口、SFTP 浏览和日志下载不直接展示 SSH.NET 原始异常消息。

本轮不得把真实主机、用户名、密码、私钥路径、私钥口令或远程文件路径写入测试文档、日志、截图和测试输出。

## 2. 测试版本与环境

| 项目 | 实际值 |
|---|---|
| 测试日期 | 2026-09-01 |
| 分支 | `main`，含当前未提交 V0.6 变更 |
| 操作系统 | Windows 11 x64 |
| SDK | .NET 10.0.400（执行时确认） |
| 配置 | Release |
| 测试服务器 | 专用 SSH/SFTP 测试服务器；文档不记录地址和凭据 |
| 测试人员 | Codex Agent |

## 3. 安全与数据隔离

1. 自动测试优先使用回环 TCP 监听器、临时目录和随机文件名。
2. 真实服务器测试只使用既有受环境变量保护的专用账号和随机远程测试目录。
3. 运行命令时不得回显环境变量值；报告只记录用例数、状态和脱敏错误类别。
4. 不修改正式服务器资料、正式 SQLite 数据或生产日志。
5. 诊断日志安全检查只搜索敏感字段名和测试哨兵，不输出日志全文。
6. 测试结束后仅清理本轮创建的临时文件和随机远程目录。

## 4. 自动验证门禁

按顺序执行：

```powershell
git diff --check
dotnet build LiteTerm.sln -c Release --no-restore
dotnet test tests\LiteTerm.Tests\LiteTerm.Tests.csproj -c Release --no-build --filter "FullyQualifiedName~ConnectionFailureTests|FullyQualifiedName~FileConnectionDiagnosticLoggerTests|FullyQualifiedName~SshTerminalSessionTests"
dotnet test LiteTerm.sln -c Release --no-build
```

若 `--no-restore` 因新增依赖缺失失败，本轮没有新增包，不应直接联网还原；先确认本地 SDK 和既有资产状态。

## 5. 测试用例

### A. 静态与构建

| 编号 | 优先级 | 测试内容 | 预期 |
|---|---|---|---|
| STA-01 | P0 | `git diff --check` | 无空白错误。 |
| STA-02 | P0 | 全部变更文件 BOM 检查 | UTF-8 无 BOM。 |
| STA-03 | P0 | 搜索 SFTP/日志下载的 `exception.Message` | 不再向用户直接展示底层异常。 |
| BLD-01 | P0 | Release 构建及 WPF/XAML 编译 | 0 错误、0 警告。 |

### B. 错误模型与会话状态

| 编号 | 优先级 | 测试内容 | 预期 |
|---|---|---|---|
| ERR-01 | P0 | 所有 `ConnectionFailureKind` 元数据 | 错误代码稳定、中文提示非空、重试属性符合设计。 |
| ERR-02 | P0 | 回环挂起握手超时 | 状态变为 `Failed`，分类为 `Timeout`，日志操作为 SSH/Connect。 |
| ERR-03 | P0 | 回环挂起握手主动取消并重试两次 | 状态回到 `Disconnected`，`LastFailure` 为空，不记录故障。 |
| ERR-04 | P1 | 诊断日志器抛错 | 不覆盖原始连接异常，仍可释放会话。 |
| ERR-05 | P1 | `Failed` 状态事件顺序 | 事件处理器读取到的 `LastFailure` 已完整设置。 |
| ERR-06 | P1 | 主机身份验证拒绝 | 分类为 `HostKeyRejected`，提示不包含原始异常消息。 |
| ERR-07 | P1 | 错误认证 | 分类为 `AuthenticationRejected`，日志不包含认证信息。 |

### C. 诊断日志

| 编号 | 优先级 | 测试内容 | 预期 |
|---|---|---|---|
| LOG-01 | P0 | 写入单条诊断事件 | JSON Lines 可读，UTF-8 无 BOM。 |
| LOG-02 | P0 | 检查字段集合 | 仅含 UTC 时间、协议、操作、错误代码和异常类型。 |
| LOG-03 | P0 | 敏感信息扫描 | 不含主机、用户名、密码、口令、私钥路径和本地/远程路径。 |
| LOG-04 | P1 | 超过大小阈值后继续写入 | 当前文件轮转为单个 `.previous`，新文件继续写入。 |
| LOG-05 | P1 | 并发写入 | 每条事件保持独立 JSON 行，无交叉或损坏。 |

### D. 全量与条件集成

| 编号 | 优先级 | 测试内容 | 预期 |
|---|---|---|---|
| REG-01 | P0 | 完整非外部测试集 | 全部通过，外部条件测试按环境明确跳过。 |
| INT-01 | P0 | 真实 SSH UTF-8、取消写入和释放 | 既有条件集成测试通过。 |
| INT-02 | P0 | 真实 SFTP 枚举、传输、取消、权限错误 | 既有条件集成测试通过；不启用 1 GiB 测试。 |
| INT-03 | P1 | 真实错误认证及日志脱敏 | 分类正确，日志中无凭据或连接参数。 |
| INT-04 | P1 | 远端主动断开后手动重连 | 原标签进入失败状态，网络恢复后可手动重连。 |

### E. WPF 界面冒烟

| 编号 | 优先级 | 测试内容 | 预期 |
|---|---|---|---|
| UI-01 | P0 | 启动最新 Release | 主窗口和 WebView2 终端正常加载，无未处理异常。 |
| UI-02 | P0 | 本地挂起握手超时 | 状态栏显示安全超时提示，弹窗不含底层异常、地址或凭据。 |
| UI-03 | P1 | 失败后的“重连”入口 | 原标签保留重连入口，不新增标签。 |
| UI-04 | P1 | SFTP 运行中断 | SFTP 操作禁用并显示安全断线原因，窗口仍可关闭释放。 |
| UI-05 | P1 | 诊断文件生成 | 应用数据日志文件产生对应脱敏事件。 |

## 6. 通过标准

- `STA-01`～`STA-03`、`BLD-01`、`ERR-01`～`ERR-03`、`LOG-01`～`LOG-03`、`REG-01` 必须通过。
- 可用测试服务器时，`INT-01` 和 `INT-02` 必须通过；环境不可用时只能记为阻塞或条件跳过。
- UI 用例不得出现崩溃、卡死、凭据泄露或原始异常直出。
- 任一失败必须先定位根因；仅修改与本功能直接相关的代码，并重跑受影响测试和完整回归。

## 7. 执行结果

| 用例 | 状态 | 结果/证据 |
|---|---|---|
| STA-01 | 通过 | `git diff --check` 无错误。 |
| STA-02 | 通过 | 全部变更及新增文本文件 UTF-8 BOM 检查通过。 |
| STA-03 | 通过 | SFTP 浏览与日志下载未发现 `exception.Message` 直出；诊断契约未包含敏感字段。 |
| BLD-01 | 通过 | 首次发现并修复 CS1501；刷新依赖后 Release 构建 0 警告、0 错误，WPF/XAML 编译通过。 |
| ERR-01～ERR-07 | 通过 | 元数据、异常映射、内层异常、主机身份拒绝、握手超时、主动取消/重试、失败事件顺序和日志器故障隔离测试均通过。真实错误认证另受 INT 环境阻塞。 |
| LOG-01～LOG-05 | 通过 | 单条写入、UTF-8 无 BOM、固定字段、敏感字段、单归档轮转及 20 路并发完整 JSON Lines 测试均通过。 |
| REG-01 | 通过 | 最终完整回归 146 项通过、0 失败，9 项 SSH/SFTP 外部条件测试因进程无法读取测试环境变量而跳过。 |
| INT-01～INT-04 | 阻塞 | 普通获准进程与 Windows PowerShell 5.1 桌面用户方式均未读取到用户级测试变量；两次均为 9 项条件跳过，无测试失败或敏感输出，未继续重复尝试。 |
| UI-01 | 通过 | 最新 Release 主窗口启动成功，WebView2/xterm.js 加载完成并显示“终端已就绪”；关闭后无残留 LiteTerm 窗口。 |
| UI-02～UI-05 | 阻塞 | Windows 自动化无法将焦点从服务器搜索框可靠切换到连接参数输入框，无法安全完成回环超时、重连和诊断文件生成；SFTP 中断同样需可控连接输入，未记为通过。 |

## 8. 缺陷记录

- `BLD-01`：SFTP 建连失败路径调用三参数失败收敛方法，而实现漏掉主机身份拒绝参数。已补齐可选参数并传入统一映射器，等待重测。
- `BLD-01` 重测：Release 构建 0 警告、0 错误，缺陷关闭。
- 依赖还原：沙箱内无法访问 NuGet，首次还原失败；获准联网后四个项目还原成功，未修改依赖版本声明。
- 真实集成首次尝试：进程未读取到用户级 SSH 测试变量，9 项全部条件跳过，未产生测试失败或敏感输出。
- 真实集成替代尝试：Windows PowerShell 5.1 桌面用户方式仍未读取到测试变量，结果相同；停止重复尝试并记为环境阻塞。
- WPF 冒烟：最新 Release 启动和 WebView2/xterm.js 就绪通过；输入焦点自动化限制阻塞 UI-02～UI-05。
- 补充专项测试首次编译误用了不存在的 `ConnectionOperation.Receive`，改为既有 `Transport` 契约后通过。
- 并发日志测试首次按 camelCase 查询字段，而默认序列化使用 `FailureCode`；修正测试断言后通过，生产格式未改变。
