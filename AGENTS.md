# LiteTerm Agent 协作说明

本文件适用于仓库根目录及其全部子目录。所有参与本项目的 Agent 在开始工作前都应先阅读本文件，并遵循其中的范围、技术约束和进度维护规则。

## 1. 项目目标

LiteTerm 是一款面向 Windows 的轻量 SSH/SFTP 客户端，优先解决以下场景：

- 保存并快速连接 Linux 服务器。
- 使用完整终端执行命令、查看实时日志。
- 通过 SFTP 浏览、上传、下载和管理远程文件。
- 通过常用日志入口和命令模板减少重复操作。

详细范围和里程碑以 `LiteTerm_开发计划.md` 为准。除非用户明确要求，不主动扩展到 RDP、VNC、X11、Telnet、串口、FTP、云同步或批量运维平台。

## 2. 当前技术方向

- 平台：Windows 10/11 x64。
- 桌面框架：.NET 10 + WPF。
- 架构：MVVM；初期保持适度分层，避免为尚未出现的需求提前抽象。
- SSH/SFTP：SSH.NET。
- 终端：WebView2 承载本地 xterm.js 静态资源。
- 数据：SQLite。
- 凭据保护：Windows DPAPI。
- 测试：单元测试、SSH/SFTP 集成测试和真实终端兼容性测试。

在技术验证阶段，优先采用以下精简结构：

```text
LiteTerm.App
LiteTerm.Core
LiteTerm.Infrastructure
LiteTerm.Tests
```

只有在职责和依赖边界已经稳定后，才继续拆分独立的 Application、Domain 或 Terminal 项目。

## 3. 开发优先级

始终优先完成可验证的纵向链路：

```text
创建连接
  -> SSH 建立会话
  -> 创建 ShellStream/PTY
  -> xterm.js 输入和显示
  -> PTY Resize
  -> 取消、关闭和资源释放
```

在这条链路稳定之前，不优先开发复杂主题、动画、服务器树或高级 SFTP 功能。

## 4. 实现约束

- UI 层不得直接依赖或操作 SSH.NET、SQLite、DPAPI 的具体对象。
- ViewModel 只负责编排和状态，不承载网络、数据库或文件传输实现。
- 网络、文件传输和可能阻塞的数据库操作不得运行在 UI 线程。
- 除 UI 事件处理器外，不使用 `async void`。
- 长耗时操作应支持 `CancellationToken`；确实无法取消时必须明确说明。
- SSH 终端与 SFTP 传输优先使用独立连接，避免文件传输影响交互终端。
- 终端输出必须批量跨越 WebView2 桥接，并限制 scrollback；禁止无限保存在 WPF 控件或内存集合中。
- 大文件必须流式传输，禁止整体读入内存。
- 连接、流、后台任务、事件订阅、WebView2 和 `CancellationTokenSource` 必须具有清晰、幂等的释放路径。
- 主机指纹首次连接需确认，后续不匹配时默认阻止连接。
- 密码、私钥口令和其他凭据不得以明文写入数据库、日志、异常提示或测试快照。
- WebView2 只加载应用内资源，阻止未授权的外部导航，并验证 C# 与 JavaScript 消息结构。
- Shell 路径和模板变量必须正确转义；执行生成的命令前应允许用户预览。

## 5. 代码与文件修改规则

- 修改前先检查工作区状态，保留用户已有且与任务无关的变更。
- 修改应围绕当前任务，避免顺手重构无关代码。
- 新的公共类型和关键生命周期代码应有简短、准确的说明；避免复述代码的无效注释。
- 依赖版本应集中管理或明确固定，不使用含糊的通配版本。
- xterm.js、WebView2 资源和其他第三方资产必须保留许可证信息。
- 数据库结构变更必须通过可升级的迁移实现，不假设可以删除用户数据库重建。
- 不提交密码、私钥、真实服务器地址、生产日志或本机专用配置。
- 不擅自提交 Git commit；只有用户明确要求时才提交。

## 6. 验证要求

根据修改范围执行尽可能贴近风险的验证：

- 核心业务逻辑：运行相关单元测试。
- SSH/终端链路：验证连接、输入输出、UTF-8、控制键、Resize、取消和释放。
- SFTP：验证小文件、大文件流式传输、取消、冲突和错误路径。
- UI：验证窗口缩放、不同 DPI、深浅主题及主要键盘操作。
- 安全相关：确认日志和持久化数据中没有敏感明文。

如果环境或外部服务器导致某项验证无法执行，应在交付说明和本文件的“阻塞项”中明确记录，不得把未执行描述为已通过。

## 7. 完成定义

一项功能完成时至少应满足：

- 正常流程和主要错误路径均已处理。
- UI 不被长任务阻塞。
- 取消和资源释放行为明确。
- 必要的诊断日志已加入且完成脱敏。
- 核心逻辑有相应测试，或记录暂时无法补充测试的原因。
- 已执行与风险相称的验证。
- 用户可见行为或开发状态变化已同步到相关文档。
- 本文件的项目进度已更新。

## 8. 进度维护规则

本节是后续 Agent 的项目状态入口，必须随开发进度维护。

每次完成会改变项目状态的工作后，执行以下更新：

1. 更新“最后更新”日期。
2. 更新当前阶段和总体状态。
3. 把已经完成并验证的事项移入“最近完成”；只写实际完成的结果。
4. 更新“下一步”，保持为 1～5 个按优先级排序且可以直接执行的任务。
5. 新增、解决或删除“阻塞项”和“关键决策”。
6. 如果实现改变了产品范围或里程碑，同时更新 `LiteTerm_开发计划.md`。

以下情况不需要为了制造进度而更新：只读检查、解释问题、没有落地变更的讨论，以及未完成的尝试。长期历史应转移到开发计划、发行说明或提交历史中，本文件只保留近期状态。

## 9. 项目进度

- 最后更新：2026-09-03
- 当前阶段：V0.9——发布候选版
- 总体状态：V0.2 的 SSH 终端、服务器管理和安全持久化链路已经建立，真实服务器连续 20 次连接/命令往返/断开释放已通过；V0.3 的 SFTP MVP 已完成并通过主要真实服务器自动化链路，界面仍有文件操作待人工验收；V0.4 已实现多 SSH 标签、每标签 SSH/SFTP 状态隔离、集中释放、深浅应用主题以及终端配色/字体/字号/scrollback 设置，连接取消、超时、标签关闭、主窗口退出和主题重启恢复均已实测；V0.5 已完成常用命令、按服务器日志入口和一键下载日志的最小纵向链路，自动与真实 SFTP 回归已通过，输入、保存重启、最终命令发送及下载界面仍待人工验收；V0.6 已实现原标签手动重连、SSH/SFTP 统一脱敏错误模型、安全用户提示、有界诊断日志、WebView2 初始化失败恢复、每标签可选自动重连和 SSH/SFTP 会话并发异步释放协调，最新 Release 构建及 166 项完整回归通过、9 项外部条件测试跳过；V0.9 已接入应用图标、0.9.0-preview 版本元数据和“关于”窗口，并新增每标签 Windows ConPTY 本地终端回退代码，未连接、SSH 断开或远程 Shell 正常退出后可恢复本地 PowerShell；本地终端变更尚未编译、测试或界面验收

### 最近完成

- 安装并确认 .NET 10.0.400 SDK 和 Windows Desktop Runtime 可用。
- 创建 App、Core、Infrastructure、Tests 四项目解决方案。
- 接入 SSH.NET 2026.0.0、WebView2 和本地 xterm.js 静态资源。
- 实现密码 SSH 会话、SHA-256 主机指纹确认、终端双向消息桥、PTY Resize、输出批量刷新及连接释放。
- 完成 WPF 连接界面并实际启动验证 WebView2、xterm.js 和尺寸同步。
- 完成密码/私钥认证切换、私钥文件选择和可选口令输入；凭据仅保留在当前连接会话中。
- 完成 6 项连接参数测试；解决方案构建达到 0 警告、0 错误，依赖审计未发现已知漏洞。
- 已使用测试服务器通过 OpenSSH 验证密码登录、UTF-8 中文输出、Ctrl+C、`vim`、`top` 和 `tail -F`；尚未作为 LiteTerm 应用验证结果。
- 新增受环境变量保护的 SSH.NET 集成测试，并使用测试服务器验证主机指纹回调、密码连接、80×24 PTY、UTF-8 命令与输出，以及断开释放。
- 新增应用数据目录 JSON 已知主机存储：首次确认后保存，后续完全匹配自动通过，主机密钥算法或 SHA-256 指纹变化时默认阻止连接。
- 完成 SQLite/DPAPI 依赖还原与安全升级；原生 SQLite 固定为 3.53.3，依赖审计未发现已知漏洞。
- 修复 SQLite 连接池残留测试文件句柄和 SSH 集成测试条件跳过行为；解决方案构建达到 0 警告、0 错误，16 项测试通过、1 项 SSH 集成测试按条件跳过。
- 将已知主机记录迁入版本 2 SQLite 架构；旧版 JSON 记录会幂等导入且不会覆盖数据库中的后续更新，版本 1 升级过程保留已有服务器资料。
- 应用启动改由组合根注入会话与数据接口，并已实际启动验证 SQLite 初始化、迁移及主窗口加载无崩溃。
- 使用测试服务器完成 LiteTerm UI 密码登录、Ubuntu 中文欢迎输出、窗口最大化后 PTY 从 140×30 同步至 310×68、主动断开、控件恢复和再次连接验证；再次连接通过 SQLite 已知主机记录自动校验，未重复弹出首次信任提示。
- 使用同一测试服务器再次运行 SSH.NET 集成测试，主机密钥回调、密码认证、80×24 PTY、UTF-8 命令与中文输出以及断开释放均通过；测试凭据只存在于进程环境并在结束后清除，仓库和 SQLite 均未发现密码明文。
- 将终端默认配色调整为浅灰文字与蓝黑背景，新增“终端外观”窗口，支持四套预设、系统可视化颜色面板和 `#RRGGBB` 精确输入，选色后同步更新色块与预览，并在保存后即时应用到当前终端。
- 新增版本 3 `app_setting` 迁移和终端配色跨实例持久化；构建达到 0 警告、0 错误，21 项测试通过、1 项 SSH 集成测试按条件跳过；已实际验证文字色和背景色面板、选色同步、保存即时应用及重启恢复。
- 在主窗口加入已保存服务器侧栏和服务器资料窗口，支持密码/私钥资料新增、编辑、删除及按钮或双击快速连接；公开资料与 DPAPI 凭据使用同一 SQLite 事务保存，快速连接成功后更新最近连接时间；23 项测试通过、1 项 SSH 集成测试按条件跳过，并已实际启动验证侧栏、资料窗口、终端加载及最大化布局。
- 服务器资料名称留空时自动使用“用户名@主机”；快捷连接区新增默认选中的“保存至本地连接”选项，仅在 SSH 建连成功后原子保存资料与凭据，相同主机、端口和用户名再次保存时会新建记录并生成 `(2)`、`(3)` 递增名称后缀；30 项测试通过、1 项 SSH 集成测试按条件跳过，并已使用隔离构建验证 WPF/XAML。
- 已保存服务器侧栏支持按分组展示，未指定分组时归入“未分组”；支持按名称、分组、主机和用户名进行不区分大小写的多关键词即时搜索，并显示筛选计数与本地时区的最近连接时间；40 项测试通过、1 项 SSH 集成测试按条件跳过，解决方案构建 0 警告、0 错误，已实际验证常规/最大化布局、搜索输入和计数刷新，未向正式数据库写入测试资料。
- 新增服务器资料复制，副本使用独立标识和递增名称、保留连接参数与受保护凭据并重置连接历史；侧栏可按名称或组内最近连接时间排序。解决方案构建 0 警告、0 错误，44 项测试通过、1 项 SSH 集成测试按条件跳过；尚未使用带数据界面人工验收复制和排序。
- 新增与终端连接相互独立的 SFTP 会话接口和 SSH.NET 实现，支持密码/私钥认证、SHA-256 主机指纹回调、连接取消、远端错误状态切换、幂等释放及异步目录枚举；目录项包含类型、大小、UTC 修改时间和 POSIX 权限，并过滤 `.`/`..`、按目录优先排序。
- 新增不依赖 Windows 路径规则的远程 POSIX 路径规范化、组合和上级导航，以及 SFTP 条件集成测试；主窗口在 SSH 连接后可打开独立的 SFTP 浏览窗口，支持当前目录、路径跳转、返回上级、刷新和双击进入目录，关闭窗口会取消操作并释放 SFTP 连接。
- SFTP 未连接访问和重复释放已有单元测试；Release 构建达到 0 警告、0 错误，58 项测试通过、2 项 SSH/SFTP 集成测试因未配置测试服务器环境变量按条件跳过。目录浏览界面尚未连接真实服务器人工验收。
- 新增基于异步文件流的 SFTP 上传/下载接口、不可变进度快照、取消传递和拒绝覆盖/允许覆盖冲突策略；上传和下载均先写同目录临时文件，完整成功后再发布目标，上传覆盖时保留远端备份并在替换失败时尝试恢复，避免取消或失败截断既有文件。
- 新增传输冲突、进度边界和未连接错误单元测试，以及 UTF-8 上传下载回环、覆盖和传输取消条件集成测试；Release 下 62 项测试通过、4 项 SSH/SFTP 集成测试因未配置测试服务器环境变量按条件跳过，解决方案构建 0 警告、0 错误。
- SFTP 浏览窗口新增上传与下载文件选择、同名文件覆盖确认、实时字节/百分比/平均速度进度、传输取消和完成/失败状态；传输期间禁用目录操作，窗口关闭会联动取消并等待独立 SFTP 会话释放，上传成功后自动刷新目录。
- 上传覆盖仅允许替换常规文件，窗口预检查与远端最终提交均阻止用文件覆盖目录；并发产生目标时会再次询问，拒绝覆盖按取消处理。Release 构建和 XAML 编译达到 0 警告、0 错误，62 项测试通过、4 项条件集成测试按环境跳过；界面尚未连接真实服务器人工验收。
- SFTP 会话新增创建目录、无覆盖重命名、删除文件和删除空目录接口；操作统一串行化、支持取消、校验文件/目录类型，并阻止重命名或删除根目录及当前工作目录。远程名称禁止空白、`.`、`..`、`/` 和空字符。
- SFTP 浏览窗口新增“新建目录”“重命名”“删除”操作、名称输入校验、同名冲突提示、非递归删除确认和成功后目录刷新；新增条件集成测试覆盖重复创建、错误类型删除、非空目录、重命名冲突、成功重命名与清理。Release 构建和 XAML 编译达到 0 警告、0 错误，76 项测试通过、5 项条件集成测试按环境跳过；界面尚未连接真实服务器人工验收。
- SFTP 浏览窗口支持从 Windows 拖入一个或多个本地文件并按顺序上传，文件选择器也同步支持多选；拖拽上传复用既有覆盖预检查、并发冲突二次确认、实时进度和取消链路，目录、失效路径和重复文件会被忽略并提示，取消或失败会停止剩余队列。Release 构建和 XAML 编译达到 0 警告、0 错误，76 项测试通过、5 项条件集成测试按环境跳过；拖拽交互和真实传输尚待测试服务器人工验收。
- 使用测试服务器完成 5 项既有 SSH/SFTP 集成测试，发现上传覆盖远程目录时会因临时文件落入上级目录而先暴露服务端权限异常；上传现会在传输前验证既有目标类型，并在最终提交时再次拒绝目录、符号链接和其他非常规文件，修复后既有集成测试 5 项全部通过。
- 新增显式环境变量保护的 SFTP 大文件条件集成测试，使用 1 GiB 稀疏源文件完成真实上传和下载，验证文件长度、首中尾内容标记、最终进度及私有内存增长低于 256 MiB；本轮测试用时约 4 分钟并通过，临时文件按专用随机路径清理。
- 新增真实服务器中文目录和中文文件名上传、枚举、下载回环及权限拒绝后会话继续可用验证；完整 Release 回归达到 0 警告、0 错误，82 项测试通过，1 项大文件测试因默认未显式启用按条件跳过。测试机连接信息已按用户要求保存到 Windows 用户级环境变量，未写入仓库、数据库或日志。
- 新增由 `LITETERM_TEST_SSH_STRESS_CONNECTIONS` 显式启用的 SSH 压力条件集成测试；使用测试服务器连续完成 20 次独立会话的主机指纹校验、80×24 PTY 建立、唯一命令标记往返、主动断开和异步释放，20 次指纹保持一致且测试全部通过，用时约 8 秒。
- 使用 LiteTerm 界面连接已保存的测试服务器并打开独立 SFTP 窗口：`/home/ren` 成功显示 24 项及类型、大小、权限和 UTC 时间；中文“下载”目录双击进入、空目录刷新和返回上级均通过，选择常规文件后下载、重命名和删除按钮正确启用，下载位置及新建目录对话框正常打开，最大化布局下路径栏、操作区、数据列和状态栏保持可见；关闭 SFTP 窗口后主 SSH 仍保持连接且可再次打开 SFTP。本轮未创建、修改或删除任何远程或本地文件。
- 新增多 SSH 标签首个纵向版本：快捷连接可在已有会话旁创建独立标签，标签分别持有 SSH 会话、WebView2、输出缓冲、连接取消源、PTY 尺寸和当前连接参数；标签标题显示服务器与连接状态，工具栏状态、断开和 SFTP 操作跟随当前标签，支持 `Ctrl+Shift+T` 新建与 `Ctrl+Shift+W` 关闭标签，关闭活动标签需确认并释放其资源，关闭主窗口会释放全部标签。实际测试发现并修复新建标签后 WebView2 核心已创建但 xterm.js 尚未就绪的连接竞态，连接流程现异步等待就绪并带 5 秒超时；使用测试服务器同时建立两个真实 SSH 标签，均显示欢迎输出和“已连接”，切换后状态与尺寸正确，断开第一个标签后第二个保持连接，关闭主窗口后测试进程在 1 秒内退出。隔离 Release 构建和 XAML 编译达到 0 警告、0 错误，83 项测试通过、8 项真实服务器条件测试按默认配置跳过；5～10 标签压力、标签关闭确认和每标签 SFTP 尚未验证。
- 将主窗口关闭改为先拦截退出、并行等待全部标签释放完成后再真正关闭，避免 WPF 在异步 `Closed` 处理首次让出后提前结束进程；SFTP 窗口同样在关闭前等待取消和会话释放，每个标签会登记其 SFTP 子窗口并在自身释放时联动关闭。使用测试服务器同时建立 5 个真实 SSH 标签，均完成连接并显示欢迎输出，断开中间标签后其余 4 个保持连接，关闭主窗口并行释放 4 个活动会话后约 0.36 秒退出；SFTP 联动关闭的最终界面验证因用户停止 Windows 自动化而未执行。
- 使用最新 Release 建立两个真实 SSH 标签并在第二标签打开独立 SFTP，`/home/ren` 成功显示 24 项；标签关闭确认的“否”分支保持 SSH/SFTP 活动，`Ctrl+Shift+W` 可再次触发确认。确认关闭时发现 SFTP 的异步 `Closing` 回调在原关闭过程尚未退出前再次调用 `Close()`，触发 WPF `InvalidOperationException` 并终止整个进程；现将 SFTP 和主窗口的最终关闭改为调度到当前回调返回后执行。修复后关闭第二标签会联动关闭其 SFTP，LiteTerm 进程保持运行且第一标签仍为“已连接”，关闭主窗口会释放剩余 SSH 并正常退出；复测后没有新增 LiteTerm `.NET Runtime 1026` 事件。Release 构建达到 0 警告、0 错误，83 项测试通过、8 项外部条件测试按环境跳过。回归过程中意外保存的 `ren@192.168.31.100 (2)` 本地测试资料及其加密凭据已在用户确认后通过界面删除，服务器列表恢复为 1 台。
- SSH 终端建连从同步 `Connect` 加取消时释放客户端改为 SSH.NET 原生 `ConnectAsync(CancellationToken)`；本地挂起握手测试发现旧实现无法在 5 秒内完成取消，修复后连续两次取消均迅速回到 `Disconnected`，同一会话仍可重试，握手超时也会进入 `Failed` 并可干净释放。每个终端标签新增生命周期取消令牌，关闭标签会取消待处理终端写入，后台或已关闭标签的取消/错误不会覆盖当前标签状态；连接完成前若标签已关闭则主动断开且不保存资料。新增真实服务器条件测试确认预取消写入后会话仍可执行后续命令。Release 构建达到 0 警告、0 错误，常规回归 85 项通过、9 项外部条件测试按环境跳过，另有 2 项真实 SSH 取消/UTF-8 测试在桌面用户环境通过。
- 新增 `LiteTerm_V0.4_人工测试计划.md`，包含自动验证基线、测试数据隔离规则、28 项人工用例、预期结果、清理步骤、结果表和通过标准；现已记录 Agent 实际执行结果、自动化补充证据和明确阻塞原因。
- 使用本地挂起 TCP 端点完成 LiteTerm 界面建连取消与超时：点击“取消连接”后约 1 秒恢复且无错误弹窗，未取消时按 15 秒配置进入明确失败状态。另将既有可清理副本改为 60 秒专用取消资料，验证连接中确认关闭标签会立即释放且不影响真实 SSH 标签；连接中关闭主窗口后 5 秒内进程退出，最近 10 分钟无新的 `.NET Runtime 1026`。重启后完成窗口 119×28→277×67→119×28 Resize、双真实 SSH、`Ctrl+Shift+T/W`、确认“否/是”、SFTP 24 项目录加载及标签联动关闭回归。用户随后明确要求保留专用本地资料；本轮未创建任何远程或本地测试文件。
- 扩展终端外观设置，新增字体、8～32 字号和 100～100000 行 scrollback 配置；设置通过既有固定 WebView2 消息即时应用到所有终端标签，字号变化后重新 Fit 并同步 PTY，旧版仅含配色的 SQLite JSON 会补齐新默认值。Release 构建 0 警告、0 错误，91 项测试通过、9 项外部条件测试按配置跳过；真实 WPF 窗口已确认新增字段、默认值、范围提示、预览布局和 WebView2 就绪，模态窗口输入保存因自动化焦点限制未完成。测试对话框已取消且进程已关闭，没有保存设置或服务器资料变更。
- 新增深色/浅色应用主题，主窗口、SFTP、服务器资料、远程名称和外观设置窗口统一使用可动态替换的 WPF 资源，终端配色保持独立；主题与终端外观通过同一 SQLite 事务保存。浅色主题已验证即时应用到主窗口和服务器资料子窗口，重启恢复通过，随后恢复并保存原深色主题。Release 构建 0 警告、0 错误，92 项测试通过、9 项外部条件测试按配置跳过；未新增、编辑或删除服务器资料。
- 修复深色终端黑底黑字：终端页原 CSP 的 `style-src 'self'` 阻止了 xterm DOM 渲染器生成的内联样式表和颜色属性，导致默认、ANSI 和用户设置颜色统一回退为黑色。现仅对样式放行 xterm 必需的 `'unsafe-inline'`，脚本仍只允许应用内文件，图片与网络连接仍禁止；同时保留 4.5:1 最低字形对比度和外观变化后的缓存刷新。隔离 WebView2 诊断已验证默认浅灰及 ANSI 红、绿、蓝恢复，真实 SSH 只读探针确认服务器未发送黑色或 OSC 改色序列；JavaScript 语法检查通过，Release 构建 0 警告、0 错误，92 项测试通过、9 项外部条件测试按环境跳过。
- 新增 V0.5 常用命令最小纵向链路：已连接标签可打开“快捷命令”窗口，新增、编辑、删除最多 50 条全局命令模板并保存至现有版本化 SQLite `app_setting`；内置“末尾 500 行持续追踪”和“进入日志目录”，支持唯一 `{path}` 变量、POSIX 单引号参数转义、特殊字符及前后空格保留、单行校验和实时执行预览。只有用户明确点击执行且原标签仍连接时才发送命令，关闭标签会取消设置访问且不会误发到其他标签。Release 构建与 XAML 编译 0 警告、0 错误，101 项测试通过、9 项外部条件测试按配置跳过；尚未连接真实服务器验收窗口交互和最终命令发送。
- 新增 `LiteTerm_V0.5_测试计划.md`：按自动门禁、当前命令链路、后续日志入口和一键下载分批验收，包含 34 个可记录结果的用例，重点验证默认配置、CRUD、空配置、重启恢复、预览不执行、两标签隔离，以及含中文、空格、单引号、分号和 `$()` 的远程路径不会触发命令注入；尚未实现的 LOG/DNL 用例明确标记为“待实现”，不混入当前通过率。
- 执行 V0.5 首轮测试：Release 构建和 XAML 编译 0 警告、0 错误，101 项测试通过、9 项外部条件测试按配置跳过，`git diff --check` 无错误；真实 SSH 连接后快捷命令入口正确启用，断开后禁用，两条默认命令、模板、空路径提示、浅色窗口布局、列表切换和取消不执行均通过，正式 SSH 终端的默认文字、中文及提示符颜色清晰可见。Computer Use 可聚焦模态路径框但无法写入任何文字，且安全规则禁止自动向终端注入命令，因此输入、保存、特殊路径预览及最终命令发送用例据实标记为阻塞。未修改正式常用命令配置或远程/本地文件，测试实例关闭后无残留进程。
- 新增 V0.5 按服务器常用日志入口：日志名称和绝对 POSIX 路径保存到 SQLite 版本 4 独立表，按服务器隔离并随服务器删除级联清理；复制服务器资料不复制日志入口，删除资料后仍活动的 SSH 标签会立即解除资料关联。窗口支持 CRUD、保存空集合、安全预览、一键追踪和进入目录。两种动作复用现有 `{path}` POSIX 单引号模板渲染器，并在发送前重新确认打开窗口时捕获的标签和服务器仍连接。顶部次要操作移到独立行，避免最小窗口宽度挤压连接输入。Release 构建与 XAML 编译 0 警告、0 错误，113 项测试通过、9 项外部条件测试按配置跳过，`git diff --check` 无错误；真实界面输入、重启恢复和最终发送尚未执行。
- 新增按服务器日志入口的一键下载：日志窗口应用当前编辑后可选择本地目标，确认既有文件覆盖，再由当前标签托管的独立 SFTP 会话流式下载；专用窗口显示字节数、百分比和平均速度，支持连接/传输取消，关闭下载窗口、标签或主窗口会取消并等待释放。下载复用既有本地同目录临时文件与成功后发布语义，传输期间新建目标会二次确认；远端目录、符号链接和其他非常规文件会被明确拒绝。Release 构建与 XAML 编译 0 警告、0 错误，常规回归 117 项通过、9 项外部条件测试按配置跳过，真实服务器回归 124 项通过、2 项压力/1 GiB 条件测试按配置跳过，`git diff --check` 无错误；真实界面目标选择、覆盖、进度、取消和标签联动关闭尚待人工验收。
- 新增 V0.6 原标签手动重连：每个标签在内存中保留最近一次连接参数和可选服务器关联，只有已断开或失败且存在快照时启用“重连”；重连复用原 SSH 会话对象、终端 WebView2 和标签，不创建额外标签，连接成功后继续更新已保存服务器的最近连接时间。删除服务器资料会解除活动及历史资料关联，关闭标签会显式清空含凭据的连接快照。真实界面已完成已保存服务器连接、主动断开、原标签重连、欢迎输出恢复、按钮/标题状态和应用退出释放验证；Release 构建 0 警告、0 错误，常规回归 124 项通过、9 项外部条件测试按配置跳过，真实服务器回归 131 项通过、2 项压力/1 GiB 条件测试按配置跳过。
- 新增 SSH/SFTP 统一失败分类与 `LastFailure`：覆盖超时、认证、主机身份、网络不可达、连接中断、权限、本地 I/O、远端操作和未知错误；取消与主动断开不生成失败详情，失败状态继续保留原标签手动重连入口。主窗口、SFTP 浏览和日志下载改用稳定脱敏文案，SFTP 窗口可响应运行中断状态。应用数据目录新增 1 MiB 当前文件加单归档的 UTF-8 无 BOM JSON Lines 诊断日志，只记录 UTC 时间、协议、操作、错误代码和异常类型，不记录主机、用户名、路径或凭据；新增异常映射、失败状态时序、日志故障隔离、轮转和并发测试。Release 构建 0 警告、0 错误，24 项专项测试及完整回归 146 项通过，9 项外部条件测试跳过；最新 Release 应用和 WebView2/xterm.js 启动冒烟通过。
- 补齐 WebView2 初始化失败处理：每个终端标签记录初始化中、就绪和失败状态并复用同一初始化任务，Runtime 缺失、页面就绪超时和未知失败使用稳定安全文案及诊断代码，不再展示底层异常消息；失败标签禁用 SSH 连接并提供原标签“重试终端”入口，重试使用显式 `Navigate` 强制重新加载本地页，收到 xterm.js `ready` 后清除旧失败。Release 构建 0 警告、0 错误，失败模型定向测试 3 项通过，完整回归 149 项通过、9 项外部条件测试跳过；真实 WPF 正常初始化通过，隔离副本缺失并恢复 `index.html` 已验证安全超时提示、诊断字段、连接门禁和原标签重试恢复，测试副本已清理。Runtime 缺失提示未通过卸载系统组件强制验证。
- 新增每标签可选自动重连：选项默认关闭且不跨重启，标签至少成功连接一次后仅对 `CanRetry` 的失败按 2/5/10 秒最多重试 3 次；复用原标签和内存连接快照，不为自动尝试重复弹出连接失败提示，后台标签不会抢焦点或覆盖当前标签状态。关闭选项、手动重连、关闭标签和退出应用会取消等待及活动建连，标签释放会等待重连任务结束。Release 构建 0 警告、0 错误，策略定向测试 15 项和完整回归 164 项通过、9 项外部条件测试跳过；真实 WPF 已验证默认关闭、标签隔离、保存资料连接、主动断开不重试、手动重连及活动会话退出释放，瞬态 `Failed` 后的自动循环仍待可控断线环境验收。
- SSH 与 SFTP 会话新增并发异步释放协调：首个 `DisposeAsync` 调用独占清理责任，其余并发调用等待同一完成结果，释放一开始即拒绝新业务操作，避免多个释放者争用并销毁同一 `SemaphoreSlim`；本地挂起握手期间 8 路并发释放定向测试 2 项通过，最新 Release 构建 0 警告、0 错误，完整回归 166 项通过、9 项外部条件测试跳过。
- V0.9 新增首版应用身份信息：用户从 4 款候选中选定简约明亮的 `LT` 字母标，蓝色几何字形、黄色光点和深蓝终端光标组成 256×256 ICO，作为可执行文件与主窗口图标；App 项目固定 `0.9.0-preview` 信息版本及 0.9.0.0 程序集/文件版本，主窗口新增“关于”入口，模态窗口从程序集元数据读取版本并展示产品定位。Release 构建 0 警告、0 错误，EXE 元数据和关联图标、浅色主窗口图标/入口/版本显示通过；首次界面检查发现关于窗口内容裁切，固定高度由 390 调整为 470 后重建通过，最终深浅主题布局复验因用户中止 Windows 自动化待完成。本轮未运行测试。

### 下一步

1. 对本地终端回退变更执行 Release 构建和 `WindowsPseudoConsoleSessionTests`，再验证新标签本地命令/UTF-8/控制键/Resize、SSH 连接前切换、按钮断开与远程 `exit` 后恢复、Shell 退出重启及标签/主窗口释放。
2. 完成 V0.9 “关于”窗口高度修复后的深浅主题布局复验，确认三行说明和关闭按钮完整可见；Release 构建、EXE/窗口图标、入口和 0.9.0-preview 显示已通过。
3. 在可控网络代理或人工断网条件下验证自动重连的瞬态 `Failed`、2/5/10 秒有限重试、等待/建连取消和后台标签恢复；不得通过停止远端服务或擅自修改系统网络制造条件。
4. 在可读取专用测试变量的桌面用户环境执行 `LiteTerm_V0.6_错误与诊断测试计划.md` 的 INT-01～INT-04 和 UI-02～UI-05，验证错误认证、远端断线、日志脱敏和原标签重连恢复。
5. 人工执行 `LiteTerm_V0.5_测试计划.md` 的 CMD/TPL/EXE/PST/LOG/DNL P0 用例，重点记录特殊路径无注入、按服务器隔离、保存重启、原标签最终发送，以及日志下载的覆盖、取消和标签释放结果。

### 阻塞项

- Windows 应用自动化不能向终端窗口注入命令，因此 `vim`、`top`、`tail -F`、控制键、搜索和复制粘贴仍需人工执行；不得把真实凭据写入仓库。
- SSH 建连取消、握手超时、连接中关闭标签/主窗口和终端写入取消已有界面、本地或真实服务器自动测试；该组界面生命周期用例不再阻塞。
- 服务器管理界面已完成启动、布局、搜索输入、资料编辑、最近连接时间、重启恢复和快速连接验证，但带数据的分组、完整 CRUD、复制与排序仍需使用可清理的专用测试资料人工验收。用户明确要求保留“LiteTerm 取消连接测试”资料，后续不再请求清理，除非用户改变要求。
- 测试服务器环境变量已保存到 Windows 桌面用户环境，但沙箱账户无法直接读取；后续自动化集成测试需继续在获准的桌面用户上下文中读取，且不得回显或记录密码。SFTP 自动化集成链路已覆盖目录枚举、UTF-8 上传下载、覆盖、取消、远程文件操作、中文路径、权限错误和 1 GiB 传输；Windows 自动化可以点击 WPF 子对话框并识别输入焦点，但本轮无法向新建/重命名对话框注入文本，也无法稳定定位系统文件对话框的文件名输入控件，因此文件选择与拖拽上传、实际下载、创建目录、重命名、删除、覆盖确认、进度和取消界面仍需用户手工输入后继续验收。
- 本轮 NuGet 漏洞查询因沙箱禁止向外部 NuGet 服务发送私有项目依赖元数据而未执行；本次未新增或升级依赖包，Release 构建已通过。
- 多标签代码已通过 Release 构建、非外部单元回归、5 个真实 SSH 会话的并发连接/切换/断开隔离/主窗口集中释放，以及双标签真实 SSH/SFTP 绑定、活动标签关闭确认、`Ctrl+Shift+W`、SFTP 联动关闭、建连取消、超时和主窗口退出界面验证；当前仅剩终端输入与 SFTP 文件操作界面受自动化能力限制。
- 终端字体、字号和 scrollback 已通过模型校验、SQLite 持久化、旧设置兼容、Release 构建和真实窗口布局检查；Windows 自动化无法把输入可靠路由到“终端外观”模态子窗口，因此即时应用与重启恢复仍待界面验收。
- 深色终端黑底黑字已修复并完成最终验收：隔离 WebView2 已验证默认及 ANSI 颜色恢复，本轮最新 Release 真实 SSH 欢迎输出、中文状态文本、默认文字和绿色提示符在深色背景上均清晰可见，不再依赖重启旧 Debug 实例复测。
- 常用命令配置、模板校验、路径转义、SQLite 持久化和 WPF/XAML 构建已有自动测试；真实 SSH 下入口启用/禁用、默认命令、窗口布局、列表切换、空路径提示和取消不执行已通过。Computer Use 可点击并聚焦常用命令模态窗口，但 `type_text` 和单字符按键均未写入控件，同时安全规则禁止自动向终端注入命令，因此 CRUD、特殊路径预览、保存重启和命令发送仍需人工验收；本轮未修改正式常用命令配置。
- 按服务器日志入口的模型校验、版本 3→4 迁移、原子替换、服务器隔离、级联删除、模板复用和容量边界已有自动测试；日志窗口需要输入名称与路径，而现有 Windows 模态输入自动化限制预计同样存在，因此 LOG-01～LOG-04 的真实输入、保存重启和最终命令发送需人工验收，不得据自动测试记为通过。
- 日志一键下载已复用真实服务器通过的流式 SFTP 下载、覆盖、取消和释放链路，远端非常规文件也已在基础设施层阻止；现有 Windows 自动化无法稳定定位系统保存对话框的文件名输入控件，因此 DNL-01～DNL-04 的目标选择、覆盖、下载进度、取消和标签联动关闭仍需人工验收，不得据集成测试记为通过。
- SSH/SFTP 统一错误模型与诊断日志已通过 Release 构建、异常映射与日志专项测试、完整非外部回归和应用启动冒烟；普通获准进程与 Windows PowerShell 5.1 桌面用户方式均无法读取用户级测试变量，真实错误认证和远端断线测试只能条件跳过。Windows 自动化也无法把焦点可靠切换到连接输入框，因此 UI-02～UI-05 仍需在可控桌面环境人工执行，不得记为通过。
- 每标签自动重连已通过 Release 构建、15 项策略测试、164 项完整回归以及真实 SSH 正常连接、主动断开不重试、手动重连、标签开关隔离和退出释放验证；当前没有不影响系统网络和远端服务的瞬态断线注入入口，因此 `Failed` 后实际 2/5/10 秒循环、等待/建连取消和后台自动恢复仍需可控代理或人工环境验收。

### 关键决策

- 第一阶段先验证终端纵向链路，再建设服务器管理和 SFTP。
- 初期采用四项目精简结构，待边界稳定后再考虑进一步拆分。
- SSH 终端和 SFTP 传输使用独立连接作为默认设计方向。
- 每个终端标签独立拥有 SSH 会话、WebView2、输出缓冲、连接取消源、PTY 尺寸和活动连接参数；主窗口只把工具栏操作路由到当前标签，关闭标签和关闭主窗口统一通过标签宿主的幂等异步释放路径清理资源。
- 主窗口和 SFTP 子窗口在 `Closing` 阶段先取消默认关闭，等待相关 SSH/SFTP 会话释放完成后再执行最终关闭；标签拥有其启动的 SFTP 子窗口，标签释放时必须先等待子窗口释放，避免进程退出或标签关闭早于网络资源清理。
- SFTP 会话在基础阶段串行化连接、目录操作和释放，避免 SSH.NET 客户端在 UI 并发操作下被同时使用；终端与 SFTP 共用连接参数构造逻辑，但不共用客户端或底层传输。
- SFTP 传输使用本地与远端同目录临时文件暂存，完成后再移动至目标；拒绝覆盖通过专用异常交给 UI 询问，允许覆盖的上传先备份既有远端文件并在替换失败时尝试恢复，取消和失败清理使用有界超时且不覆盖原始异常。
- SFTP 浏览窗口一次只执行一个目录或传输操作；传输使用独立的窗口级取消源并链接窗口生命周期，进度面板保留最后状态，上传覆盖只允许常规文件且在最终提交时再次校验远端目标类型。
- 多文件选择或拖拽上传按顺序复用单文件传输链路；取消、失败或窗口关闭会停止剩余文件，至少一个文件成功后统一刷新目录，避免对单个 SFTP 会话并发传输。
- SFTP 上传在创建同目录临时文件前预检既有覆盖目标，并在提交时二次校验；只有常规文件可被覆盖，目录、符号链接和其他类型统一以明确的 `IOException` 拒绝，避免目标路径为目录时误将临时文件写入不可写的上级目录。
- 1 GiB SFTP 验证作为显式启用的条件集成测试保留，通过 `LITETERM_TEST_SFTP_LARGE_FILE_BYTES` 指定大小，避免日常完整测试默认消耗大量网络、磁盘和时间。
- SSH 重复连接压力验证作为显式启用的条件集成测试保留，通过 `LITETERM_TEST_SSH_STRESS_CONNECTIONS` 指定 1～100 次，避免日常完整测试默认反复连接外部测试服务器。
- SFTP 删除在 MVP 阶段只允许文件和空目录，不提供递归删除；重命名不覆盖同名目标，根目录和当前工作目录在基础设施层默认禁止重命名或删除，避免 UI 检查被绕过后造成破坏性操作。
- 终端待发送输出按 1 MiB 设上限；过载时丢弃最旧数据并在终端中提示，避免进程内存无限增长。
- 日志一键下载使用独立于终端 SSH 的 SFTP 会话，并作为标签拥有的异步子窗口参与统一关闭；本地目标先由用户明确选择，覆盖前确认，下载写入同目录临时文件并仅在完整成功后发布，取消或失败不得截断既有目标。
- 手动重连使用标签内最近一次连接参数快照，仅在 `Disconnected` 或 `Failed` 状态启用并复用原标签会话；快照中的凭据只保留在标签内存中，标签释放时显式清空。服务器资料被删除后解除快照的资料关联，后续重连不得重新产生已删除的服务器或日志关联。
- SSH 建连使用 SSH.NET 原生 `ConnectAsync(CancellationToken)`；取消和超时继续沿用统一清理路径，标签生命周期令牌同时用于取消待处理终端写入，避免关闭标签后后台操作更新其他标签状态。
- 自动重连是每标签、默认关闭且不持久化的显式选择；仅在标签至少成功连接过一次、保留连接快照且失败允许重试时生效，最多按 2/5/10 秒执行 3 次。自动尝试复用原标签，不弹重复连接失败对话框；手动重连、关闭选项和标签生命周期均可取消等待或建连，认证、主机身份和权限失败禁止自动重试。
- SSH 与 SFTP 会话的异步释放采用一次性原子协调和共享完成信号；只有首个 `DisposeAsync` 执行串行清理，后续调用等待相同结果，且释放开始后不再接受新的连接、传输或远端文件操作。
- SSH/SFTP 会话通过统一 `ConnectionFailure` 暴露稳定错误分类和安全用户文案，并在触发 `Failed` 状态前更新 `LastFailure`；取消、主动断开和下一次连接会清空失败详情。连接诊断使用单例轻量文件日志器，日志字段固定为 UTC 时间、协议、操作、稳定错误代码和异常类型，文件达到 1 MiB 时保留一个归档，日志故障不得覆盖原始异常或阻止资源释放。
- WebView2 初始化按终端标签持有单一在途任务和可重试状态；Runtime 缺失、初始化超时与未知失败不得展示底层异常消息，统一写入既有有界诊断日志的 Terminal/Initialize 事件。终端未收到 xterm.js `ready` 前禁用 SSH 连接，初始化失败后只能由用户显式在原标签重试。
- 应用版本以 App 项目的 MSBuild 元数据为单一来源：程序集与文件版本使用四段数值，用户可见版本使用稳定的 `InformationalVersion`；关于窗口运行时读取元数据。可执行文件和 WPF 窗口共用项目内单一 ICO，避免多份图标漂移。
- 每个终端标签独立持有 Windows ConPTY 本地会话和 SSH 会话，但同一时刻只有一个输入后端：WebView2 就绪后默认启动本地 PowerShell，SSH 建连前等待本地会话停止，SSH 断开、失败或远程 Shell 正常关闭后恢复本地会话；本地与远程输出继续复用同一有界缓冲和 xterm.js，标签释放同时等待两类会话清理。
- 服务器公开资料与敏感凭据分表保存；凭据使用当前 Windows 用户范围的 DPAPI 保护后以 BLOB 写入，数据库架构通过前向迁移版本维护。
- 已知主机与服务器资料共用版本化 SQLite；升级时保留旧版 JSON 文件并以 `INSERT OR IGNORE` 导入，数据库记录优先。
- 为规避已披露的原生 SQLite 高危漏洞，显式固定 `SQLitePCLRaw.lib.e_sqlite3` 3.53.3，并将零高危依赖审计作为验证要求。
- 终端外观使用固定结构的 C#/JavaScript 消息即时应用，颜色限制为 `#RRGGBB`，字体限制为非空且最长 128 字符，字号限制为 8～32，scrollback 限制为 100～100000 行；设置统一存入版本化 `app_setting`，旧版缺少新增字段时使用默认值。
- 应用主题使用 WPF `DynamicResource` 统一驱动主窗口和子窗口颜色，深浅主题与终端外观在同一 SQLite 事务中保存；应用主题不自动改变 xterm.js 配色，避免覆盖用户独立选择的终端方案。
- xterm.js 固定启用 4.5:1 最低字形对比度，外观变化后清理字形缓存并刷新当前缓冲区；远端 ANSI 配色仍被保留，但不可读的前景/背景组合由渲染器自动校正。
- 终端 CSP 的脚本源保持为应用内 `'self'`，连接和图片保持禁止；样式源允许应用内文件与 `'unsafe-inline'`，因为 xterm DOM 渲染器必须动态生成内联样式表和颜色属性，否则所有字形颜色会回退为黑色。
- 常用命令作为全局应用设置存入现有 `app_setting`，不为纯 JSON 配置增加数据库迁移；模板当前只识别 `{path}`，展开时将完整远程路径编码为单个 POSIX shell 单引号参数并保留原始空格。窗口持续显示最终命令预览，只有用户明确执行且打开窗口时捕获的原标签仍连接才发送，避免标签切换或关闭造成误发。
- 常用日志入口只关联已保存服务器资料，使用版本 4 `server_log_entry` 表按服务器存储并随资料删除级联清理；复制服务器资料不复制日志入口，避免环境相关路径无意扩散。追踪和进入目录复用常用命令的固定模板与统一 `{path}` 渲染器。窗口打开时捕获标签和服务器标识，保存及执行前重新确认二者仍匹配，临时未保存连接不开放日志入口。
- 服务器资料与对应凭据通过仓储的单事务操作写入，避免界面保存过程中只落库公开资料或只更新凭据；切换认证方式时清除不再使用的敏感字段。
- 快捷连接默认勾选“保存至本地连接”，但只在建连成功时保存；相同主机、端口和用户名再次保存会使用新的服务器标识，并为缺省名称添加从 `(2)` 开始的首个可用递增后缀。
- 服务器资料副本使用新的服务器标识和“原名称 - 副本”递增名称，复制原资料及其 DPAPI 保护凭据，但不继承最近连接时间；排序保持分组结构，仅在各组内按最近连接时间降序排列。
