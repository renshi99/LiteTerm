# LiteTerm V0.4 人工测试计划

## 1. 目的与范围

本计划用于验收 LiteTerm 当前 V0.4 多标签与日常体验版本，重点覆盖：

- SSH 建连取消、超时、失败恢复和资源释放。
- 多标签状态隔离、快捷键、关闭确认和集中退出。
- 每标签 SFTP 绑定及文件操作。
- 真实终端兼容性、服务器资料管理和终端外观持久化。

本计划可由用户或 Codex Agent 在 Windows 10/11 x64 桌面环境执行。测试只使用专用测试服务器、专用远程目录和可删除的本地文件，不在生产目录执行上传、覆盖、重命名或删除。

## 2. 自动验证基线

开始人工测试前，当前代码已经完成以下自动验证：

- Release 构建：0 警告、0 错误。
- 常规回归：92 项通过，9 项外部条件测试按环境跳过。
- 终端字体、字号和滚动行数：范围校验、规范化、SQLite 跨实例持久化及旧版仅配色 JSON 兼容测试通过。
- 深色终端可读性：终端 CSP 已允许 xterm DOM 渲染器必需的内联样式，同时保持脚本仅允许应用内文件；默认浅灰和 ANSI 红、绿、蓝已在隔离 WebView2 中恢复。xterm.js 同时启用 4.5:1 最低字形对比度，外观变化后会清理字形缓存并刷新当前缓冲区。
- 本地挂起 SSH 握手：取消后 5 秒内返回 `Disconnected`，同一会话连续两次取消后仍可重试。
- 本地 SSH 握手超时：进入 `Failed`，随后可释放为 `Disconnected`。
- 真实 SSH：UTF-8 命令往返通过；预取消终端写入后会话仍保持连接，后续命令往返通过。

人工测试不需要重复证明上述内部断言，但需要验证对应的界面状态和用户操作路径。

## 3. 测试版本与环境

- 可执行文件：`src\LiteTerm.App\bin\Release\net10.0-windows\LiteTerm.App.exe`
- 操作系统：Windows 10 或 Windows 11 x64
- 运行时：.NET 10 Windows Desktop Runtime、WebView2 Runtime
- 测试服务器：允许 SSH/SFTP 的专用 Linux 服务器
- 测试账号：只拥有测试目录权限的非生产账号
- 建议显示环境：100% 和 150% DPI 各执行一次基础冒烟测试

记录以下信息：

| 项目 | 实际值 |
|---|---|
| 测试日期 | 2026-08-29 |
| Windows 版本 | Windows 11 x64 |
| DPI/缩放比例 | 当前桌面缩放；100%/150% 专项未切换 |
| LiteTerm 构建时间 | 本轮 Release 构建（0 警告、0 错误） |
| 测试服务器系统 | Ubuntu 22.04.5 LTS |
| 测试人员 | Codex Agent |

## 4. 测试数据与安全要求

1. 创建唯一测试标识，例如 `LITETERM-MANUAL-20260829-01`。
2. 远程操作仅限 `$HOME/liteterm-manual-<测试标识>`。
3. 本地操作仅限新建的临时目录，例如 `C:\Temp\LiteTerm-Manual-<测试标识>`。
4. 准备以下本地文件：
   - 小型 UTF-8 文本文件，内容包含中文、英文和 emoji。
   - 两个同名但内容不同的覆盖测试文件。
   - 100 MiB～1 GiB 的非敏感大文件，用于进度和取消。
   - 两个文件用于多选及拖拽上传。
5. 除服务器资料专项外，快捷连接时取消勾选“保存至本地连接”。
6. 不在截图、缺陷记录、日志或测试文档中记录密码、私钥内容或私钥口令。
7. 删除、覆盖和重命名前再次确认当前路径位于专用测试目录。

## 5. 结果标记

- `通过`：实际结果完全符合预期。
- `失败`：功能错误、状态错误、崩溃、卡死或资源未释放。
- `阻塞`：因环境或权限无法执行，必须记录原因。
- `跳过`：经确认本轮不执行，必须记录批准原因。

缺陷至少记录：用例编号、复现步骤、实际结果、预期结果、截图、发生时间、是否可稳定复现。不得附带真实凭据。

## 6. 测试用例

### A. 启动与基础冒烟

#### SMK-01 启动与终端初始化（P0）

步骤：

1. 启动 Release 版 LiteTerm。
2. 等待首个标签加载完成。
3. 最大化、还原并调整窗口大小。

预期：

- 应用无崩溃，首个标签显示“终端已就绪”。
- 主机、端口、用户名和认证区域可操作。
- 终端区域随窗口变化，状态栏尺寸同步更新。

#### SMK-02 已保存服务器快速连接（P0）

步骤：选择测试服务器，点击“连接”，等待欢迎输出，再点击“断开”。

预期：连接成功后状态为“已连接”，终端可输入；断开后恢复可连接状态，不重复询问已信任且未变化的主机指纹。

### B. 建连取消、超时与失败恢复

#### CNL-01 通过“取消连接”中止挂起握手（P0）

准备：在独立 PowerShell 窗口运行以下本地挂起端点；测试结束后按 `Ctrl+C`：

```powershell
$listener = [Net.Sockets.TcpListener]::new([Net.IPAddress]::Loopback, 22222)
$listener.Start()
Write-Host "Listening on 127.0.0.1:22222"
try {
    $client = $listener.AcceptTcpClient()
    Start-Sleep -Seconds 60
} finally {
    if ($client) { $client.Dispose() }
    $listener.Stop()
}
```

步骤：

1. 在 LiteTerm 输入主机 `127.0.0.1`、端口 `22222`、任意测试用户名和测试密码。
2. 取消勾选“保存至本地连接”。
3. 点击“连接”，确认按钮变为“取消连接”。
4. 在 2 秒内点击“取消连接”。

预期：

- 状态先显示“正在取消连接…”，随后在 5 秒内显示“已取消连接”或恢复为可连接状态。
- 界面不卡死，不出现“连接失败”弹窗。
- 可继续使用其他标签并连接真实测试服务器。

#### CNL-02 挂起握手超时（P0）

步骤：重新启动 CNL-01 的本地端点并连接，但不点击取消，等待超过连接超时时间。

预期：连接在配置的超时范围后失败，出现明确失败提示；连接表单恢复可用，应用不崩溃。

#### CNL-03 连接中关闭标签（P0）

步骤：

1. 保持一个真实 SSH 标签已连接。
2. 新建标签并连接 CNL-01 的挂起端点。
3. 在“连接中”关闭新标签，确认继续。

预期：挂起标签在 5 秒内关闭；原真实 SSH 标签仍为“已连接”且可执行命令；无错误弹窗和崩溃。

#### CNL-04 连接中关闭主窗口（P0）

步骤：连接 CNL-01 的挂起端点后立即关闭主窗口。

预期：应用在 5 秒内退出，无残留 LiteTerm 进程，无 `.NET Runtime 1026` 崩溃事件。

#### CNL-05 取消后重新连接（P1）

步骤：完成 CNL-01 后，使用同一窗口连接真实测试服务器，执行唯一标记命令并断开。

预期：新连接成功，命令输出正确，主动断开后状态恢复正常。

### C. 多标签与状态隔离

#### TAB-01 快捷键与关闭确认（P0）

步骤：

1. 使用 `Ctrl+Shift+T` 创建新标签。
2. 连接真实服务器。
3. 使用 `Ctrl+Shift+W` 关闭活动标签。
4. 在确认框先选择“否”，再次关闭后选择“是”。

预期：“否”保持会话；“是”断开并关闭标签；最后一个标签关闭后自动创建新的空白标签。

#### TAB-02 两标签会话隔离（P0）

步骤：同时连接两个标签，分别执行不同唯一标记命令；断开其中一个，再在另一个执行命令。

预期：输出不串标签；断开一个标签不改变另一个标签的状态、终端内容和操作能力。

#### TAB-03 五至十标签压力（P1）

步骤：创建并连接 5 个标签；条件允许时增加至 10 个，连续切换、调整窗口大小并执行短命令。

预期：标签切换和输入无明显卡顿；标题、连接状态和 PTY 尺寸属于各自标签；进程内存没有持续无界增长。

#### TAB-04 后台标签错误不污染当前标签（P1）

步骤：保持当前标签正常连接，使另一个后台标签断开或连接失败。

预期：当前标签状态栏和按钮仍反映当前会话，后台错误不会把当前标签显示为失败。

### D. 每标签 SFTP 与关闭生命周期

#### SFT-01 SFTP 绑定当前标签（P0）

步骤：连接两个 SSH 标签，分别打开 SFTP，确认标题和默认路径；在两个 SFTP 窗口分别刷新目录。

预期：每个 SFTP 使用所属标签的服务器参数，窗口和目录互不串用；SFTP 操作不影响终端交互。

#### SFT-02 关闭 SSH 标签联动关闭 SFTP（P0）

步骤：在第二标签打开 SFTP并加载完成，关闭第二 SSH 标签并确认。

预期：第二标签及其 SFTP 一并关闭；第一标签和其 SFTP 保持可用；进程不崩溃。

#### SFT-03 主窗口集中释放（P0）

步骤：保持至少 3 个 SSH 标签和 2 个 SFTP 窗口活动，关闭主窗口。

预期：所有窗口在 5 秒内退出，无残留 LiteTerm 进程和新的 `.NET Runtime 1026` 事件。

### E. SFTP 文件操作

#### FIL-01 路径与中文目录（P0）

步骤：进入专用测试目录，创建中文子目录，执行进入、刷新、返回上级和路径跳转。

预期：路径始终为远程 POSIX 格式；中文名称显示正确；项目数、类型、权限和 UTC 时间正确刷新。

#### FIL-02 上传、下载与内容校验（P0）

步骤：上传 UTF-8 小文件，下载到新的本地路径，使用本地工具比较文件大小和内容。

预期：上传下载成功，内容完全一致，进度最终为 100%，SSH 终端在传输期间保持可用。

#### FIL-03 覆盖确认（P0）

步骤：上传同名不同内容文件，先拒绝覆盖，再允许覆盖并下载校验。

预期：拒绝时原文件不变；允许时目标完整替换；目录、符号链接或其他非常规目标不能被文件覆盖。

#### FIL-04 多选与拖拽上传（P1）

步骤：通过文件选择器多选上传两个文件，再从 Windows 拖入两个文件。

预期：文件按顺序上传；无效路径、目录和重复文件被忽略并提示；成功后目录统一刷新。

#### FIL-05 大文件进度与取消（P0）

步骤：上传或下载 100 MiB～1 GiB 文件，传输过程中点击取消；刷新远程和本地目录。

预期：取消响应明确；既有目标文件不被截断；临时文件不作为最终目标暴露；取消后 SFTP 会话仍可刷新并传输小文件。

#### FIL-06 新建、重命名与删除（P0）

步骤：在专用测试目录创建子目录，上传文件，重命名文件，删除文件，再删除空目录；尝试删除非空目录。

预期：正常操作成功并刷新；重命名不覆盖同名目标；非空目录删除被拒绝；根目录和当前工作目录不能被重命名或删除。

### F. 真实终端兼容性

#### TRM-01 UTF-8、控制键与长任务（P0）

步骤：执行中文输出命令；运行持续输出命令后按 `Ctrl+C`；确认提示符恢复。

预期：中文无乱码；输出实时；`Ctrl+C` 能中断前台任务且会话继续可用。

#### TRM-02 全屏终端程序（P1）

步骤：分别打开 `vim` 和 `top`，执行基本移动、输入和退出。

预期：方向键、功能键、控制序列和屏幕刷新正确，无明显残影或错位。

#### TRM-03 `tail -F`、Resize 与复制粘贴（P1）

步骤：运行 `tail -F` 测试日志；调整窗口大小；测试终端选择、复制、粘贴和搜索。

预期：实时输出持续；Resize 后内容和光标位置正常；复制粘贴及搜索符合 xterm.js 行为。

### G. 服务器资料与外观

#### SRV-01 服务器资料 CRUD 与凭据（P0）

步骤：创建带唯一测试标识的服务器资料，编辑、快速连接、复制，再删除副本和原资料。

预期：公开资料与凭据同步保存；复制使用新标识且不继承最近连接时间；删除资料时凭据一并删除；其他资料不受影响。

#### SRV-02 分组、搜索、排序和重启恢复（P1）

步骤：创建至少 3 条可清理资料并设置不同分组；测试多关键词搜索、名称排序、最近连接排序；重启应用。

预期：分组、筛选计数、排序和最近连接时间正确；重启后资料与排序所需数据仍存在。

#### APP-01 终端外观保存与标签应用（P1）

步骤：分别选择预设颜色和自定义 `#RRGGBB` 颜色，保存后检查多个标签并重启应用。

预期：当前及后续标签即时应用；颜色输入校验明确；重启后设置恢复。

#### APP-02 字体、字号与滚动行数（P1）

步骤：将字体改为 `Consolas, monospace`、字号改为 `18`、滚动行数改为 `12000`，保存后检查当前和新建标签的显示与 PTY 尺寸，再重启应用；最后恢复默认值。

预期：预览与所有标签即时应用字体和字号，字号变化后重新计算 PTY 尺寸；滚动历史受配置上限约束；非法范围显示明确提示；重启后恢复已保存值，旧版仅配色设置自动使用新字段默认值。

#### APP-03 应用深浅主题（P1）

步骤：切换为浅色主题并保存，检查主窗口和服务器资料子窗口，再重启应用；最后切回并保存深色主题。

预期：当前主窗口和已打开或新建子窗口即时应用主题；终端配色保持独立；重启后主题恢复；切回深色后布局、文字对比度和功能状态正常。

#### APP-04 深色终端可读性（P0）

步骤：使用深色终端连接测试服务器，检查登录欢迎信息和提示符；执行 ANSI 黑色以及常用红、绿、黄、蓝、青色输出；打开外观设置保存一次当前配色，再检查已有输出和新输出。

预期：默认文字清晰可见；ANSI 黑色在深色背景上自动提升至至少 4.5:1 对比度；常用颜色仍可区分；保存外观后已有缓冲区立即刷新，不再出现黑底黑字。

### H. 安全与清理

#### SEC-01 敏感信息检查（P0）

步骤：完成一次密码连接、失败连接和私钥连接后，检查用户可见错误信息、应用日志和测试截图。

预期：不出现密码、私钥口令或私钥内容；主机指纹变化时默认阻止连接。

#### CLN-01 测试数据清理（P0）

步骤：确认路径后删除专用远程测试文件和目录，删除本地临时目录及带测试标识的服务器资料。

预期：只删除本轮专用测试数据；原有服务器资料、远程文件和已知主机记录不受影响。

## 7. 测试结果汇总

| 用例 | 结果 | 实际结果/缺陷编号 |
|---|---|---|
| SMK-01 | 通过 | Release 版启动正常；还原尺寸为 119×28，最大化后同步为 277×67，恢复后回到 119×28。 |
| SMK-02 | 通过 | 已保存测试服务器快速连接成功、欢迎输出正常、重复连接未再次询问已知主机。 |
| CNL-01 | 通过 | TCP 已接受后点击“取消连接”，约 1 秒恢复“已断开”，无失败弹窗和卡死。 |
| CNL-02 | 通过 | 挂起握手约 15 秒后明确提示连接失败，表单恢复，应用未崩溃。 |
| CNL-03 | 通过 | 使用 60 秒专用本地资料，在连接中确认关闭标签后立即关闭且无错误提示；原真实 SSH 标签仍为“已连接”。 |
| CNL-04 | 通过 | 连接中关闭主窗口后 5 秒内进程退出；最近 10 分钟无新的 `.NET Runtime 1026`。 |
| CNL-05 | 通过 | 取消/超时测试后真实服务器重新连接成功；唯一标记命令与写入取消由真实 SSH 集成测试覆盖。 |
| TAB-01 | 通过 | `Ctrl+Shift+T` 新建标签；`Ctrl+Shift+W` 的“否”保持会话，“是”关闭活动标签。 |
| TAB-02 | 通过 | 两个真实 SSH 标签同时连接；关闭第二标签后第一标签仍连接，命令输出隔离由真实 SSH 集成测试补充验证。 |
| TAB-03 | 通过 | 已完成 5 个真实 SSH 会话并发连接、切换、单会话断开隔离和集中释放；10 标签未扩展执行。 |
| TAB-04 | 通过 | 本地挂起/关闭标签期间切回真实标签，状态栏及按钮保持真实标签的“已连接”状态。 |
| SFT-01 | 通过 | 双真实 SSH 标签场景中，第二标签打开独立 SFTP 并加载 `/home/ren` 24 项；既有双窗口绑定回归结果继续有效。 |
| SFT-02 | 通过 | 关闭拥有 SFTP 的第二标签后，SFTP 窗口同步消失，第一 SSH 标签仍连接，进程无崩溃。 |
| SFT-03 | 通过 | 既有 3 SSH/2 SFTP 集中释放验证通过；本轮连接中主窗口关闭再次确认无残留进程和 1026 事件。 |
| FIL-01 | 通过 | 真实 SFTP 显示 `/home/ren` 24 项，中文目录、类型、权限和 UTC 时间正常；中文目录导航沿用既有界面验收结果。 |
| FIL-02 | 阻塞 | UTF-8 上传下载回环集成测试已通过；Windows 自动化无法稳定操作系统文件对话框，本轮未重复 UI 实传。 |
| FIL-03 | 阻塞 | 覆盖/拒绝覆盖/目录保护集成测试已通过；本轮未在 UI 创建远程测试目标。 |
| FIL-04 | 阻塞 | 多文件选择与拖拽代码、构建均通过；Windows 自动化无法安全完成系统文件选择与拖拽输入。 |
| FIL-05 | 阻塞 | 1 GiB 流式传输与取消集成测试已通过；本轮未从 UI 发起大文件传输。 |
| FIL-06 | 阻塞 | 创建、重命名、删除及错误路径集成测试已通过；本轮未创建远程数据，避免在无专用目录确认下执行删除。 |
| TRM-01 | 阻塞 | UTF-8 往返、写入取消后会话可用已由真实 SSH 集成测试通过；Windows 自动化安全规则禁止向终端注入命令。 |
| TRM-02 | 阻塞 | `vim`、`top` 需人工键盘操作，Windows 自动化安全规则禁止向终端输入。 |
| TRM-03 | 阻塞 | Resize 已通过；`tail -F`、复制粘贴和搜索需人工终端输入。 |
| SRV-01 | 阻塞 | 成功将既有意外副本改为专用取消测试资料并验证保存/重启恢复；复制和最终删除尚未执行。 |
| SRV-02 | 阻塞 | 名称、地址、超时及凭据修改在重启后恢复；本轮未创建 3 条分组资料完成全量排序验收。 |
| APP-01 | 通过 | 预设、自定义颜色、即时应用及重启恢复已在当前 V0.4 代码基线完成界面验收。 |
| APP-02 | 阻塞 | 新增输入框、范围提示、默认值、预览布局及 WebView2 就绪已在 Release 界面确认；Windows 自动化无法把键盘焦点可靠路由到模态子窗口，未执行输入、保存和重启恢复。范围校验、持久化、旧设置兼容及消息字段已由自动测试和构建覆盖。 |
| APP-03 | 通过 | 浅色主题保存后主窗口即时更新，服务器资料子窗口同步使用浅色资源；重启后仍为浅色。随后已切回并保存原深色主题，终端配色保持独立，未修改服务器资料。 |
| APP-04 | 阻塞 | 用户截图确认旧 CSP 下前景像素为 `#000000`、背景为 `#050816`；只读 SSH 探针排除远端黑色/OSC 改色，隔离 WebView2 对照确认 CSP 阻止 xterm 内联样式是根因，放行样式后默认浅灰与 ANSI 红、绿、蓝均恢复。JavaScript 检查、Release 构建和常规回归通过，当前 Debug 实例需重启后完成真实连接最终复测。 |
| SEC-01 | 通过 | 成功连接、取消、超时和失败提示均未显示密码；自动测试与仓库检查未发现凭据明文。 |
| CLN-01 | 跳过 | 本轮未创建远程或本地文件；用户明确要求保留本地专用资料“LiteTerm 取消连接测试”，不执行删除。 |

### 本轮执行说明

- 两次 15 秒连接中关闭尝试因自动化工具决策间隔超过连接超时，作为无效样本不计入结果；改用 60 秒专用资料后 CNL-03 一次通过。
- Computer Use 安全规则禁止自动向终端应用输入命令，因此 TRM 用例不能以界面自动化替代人工输入。
- 上传、下载、覆盖和删除的基础设施真实服务器集成测试已通过，但没有把未执行的 UI 操作描述为人工通过。
- 终端外观模态窗口可正常显示新增字段；自动化输入会错误路由到主窗口，因此已取消对话框并关闭测试实例，未保存任何设置或服务器资料变更。

## 8. 通过标准

- 所有 P0 用例必须通过。
- P1 用例不得存在导致崩溃、数据损坏、凭据泄露或会话串扰的问题。
- 关闭标签、关闭 SFTP 和关闭主窗口后不得残留 LiteTerm 进程。
- 测试结束后专用远程目录、本地临时目录和测试服务器资料均已清理。
- 失败用例必须有可复现步骤；修复后重新执行失败用例及相关 P0 回归。
