# LiteTerm 开发计划

> 一款面向 Windows 的精简 SSH/SFTP 客户端，聚焦“登录远程服务器查看日志”和“上传下载文件”两个高频场景。

## 1. 项目定位

### 1.1 项目目标

LiteTerm 不复刻 MobaXterm 的全部能力，只解决日常运维和开发排查中最常见的问题：

1. 快速保存并连接 Linux 服务器。
2. 在稳定、完整的终端中执行命令和查看实时日志。
3. 通过 SFTP 浏览、上传、下载和管理远程文件。
4. 通过常用日志路径、命令模板等功能减少重复操作。
5. 保持启动快、占用低、界面简单、配置安全。

### 1.2 目标用户

- 后端开发人员
- 测试及运维人员
- 需要频繁查看 Linux 日志、替换配置文件或上传发布包的个人用户

### 1.3 1.0 版本范围

**必须具备：**

- SSH 密码登录
- SSH 私钥登录
- 服务器新增、编辑、删除、分组和搜索
- 多标签页终端
- 完整 ANSI/VT 终端显示
- SFTP 文件浏览、上传、下载、重命名、删除、新建目录
- 文件拖拽上传
- 传输进度、取消与失败提示
- 常用命令和常用日志入口
- SSH 断线检测和手动重连
- 主机指纹确认
- 密码加密保存
- 深色/浅色主题及基础终端设置

**明确不做：**

- X11 Server
- RDP、VNC
- Telnet、串口
- 本地 Linux/Cygwin 工具环境
- FTP
- 团队配置同步
- 云账号体系
- 复杂日志分析平台
- 批量运维和脚本编排平台

---

## 2. 技术方案

### 2.1 推荐技术栈

| 层次 | 技术 | 用途 |
|---|---|---|
| 桌面框架 | .NET 10 + WPF | Windows 主程序和界面 |
| 架构模式 | MVVM | 分离界面、状态和业务逻辑 |
| SSH/SFTP | SSH.NET | SSH 会话、交互式 Shell、SFTP |
| 终端渲染 | xterm.js | ANSI/VT 控制序列、光标、颜色、滚动缓冲 |
| Web 宿主 | WebView2 | 在 WPF 中承载 xterm.js |
| 本地数据 | SQLite | 保存服务器、分组、日志配置和应用设置 |
| 凭据保护 | Windows DPAPI | 加密保存密码、私钥口令等敏感信息 |
| 日志 | Microsoft.Extensions.Logging 或轻量日志实现 | 记录应用错误和诊断信息 |
| 打包 | 自包含安装包或压缩包 | 发布 Windows x64 版本 |

### 2.2 总体结构

```text
LiteTerm
├─ LiteTerm.App              # WPF 入口、Views、ViewModels
├─ LiteTerm.Application      # 用例、会话管理、业务编排
├─ LiteTerm.Domain           # 实体、枚举、接口
├─ LiteTerm.Infrastructure   # SSH.NET、SQLite、DPAPI 实现
├─ LiteTerm.Terminal         # WebView2、xterm.js、终端桥接
└─ LiteTerm.Tests            # 单元测试与集成测试
```

### 2.3 运行架构

```text
┌─────────────────────────────────────────────┐
│                 WPF 主界面                   │
│  服务器列表 / 标签页 / SFTP / 设置 / 传输队列 │
└───────────────┬─────────────────────────────┘
                │ MVVM / Application Services
┌───────────────▼─────────────────────────────┐
│                Session Manager              │
│  SSH 生命周期 / SFTP 生命周期 / 重连 / 取消   │
└───────────┬───────────────────┬─────────────┘
            │                   │
┌───────────▼──────────┐  ┌─────▼─────────────┐
│ SSH.NET ShellStream  │  │ SSH.NET SftpClient│
└───────────┬──────────┘  └───────────────────┘
            │
┌───────────▼─────────────────────────────────┐
│ WebView2 ↔ C# 消息桥 ↔ xterm.js             │
│ 输入、输出、窗口尺寸、复制粘贴、搜索          │
└─────────────────────────────────────────────┘
```

---

## 3. 核心模块设计

## 3.1 服务器管理

服务器配置字段：

```text
名称
分组
主机/IP
端口
用户名
认证方式
密码密文或私钥路径
私钥口令密文
默认远程目录
连接超时
KeepAlive 间隔
备注
最近连接时间
主机指纹
```

基础功能：

- 新增、编辑、复制、删除服务器
- 按分组展示
- 按名称、IP、用户名搜索
- 双击快速连接
- 最近连接列表
- 导入、导出非敏感配置

设计原则：

- 数据库中不保存明文密码。
- 私钥优先保存文件路径，不直接复制私钥内容。
- 导出配置时默认排除密码、私钥口令和主机指纹敏感信息。

## 3.2 SSH 终端

核心链路：

```text
键盘输入
  ↓
xterm.js onData
  ↓
WebView2 postMessage
  ↓
C# TerminalBridge
  ↓
SSH.NET ShellStream.Write
  ↓
远程 Linux PTY
```

```text
远程 Linux 输出
  ↓
ShellStream
  ↓
后台读取循环
  ↓
输出缓冲与批量刷新
  ↓
WebView2 postMessage
  ↓
xterm.js.write
```

必须解决：

- UTF-8 中文显示和输入
- ANSI 颜色与光标控制
- `vim`、`less`、`top`、`htop`、`tmux` 基础兼容
- PTY 初始尺寸
- 窗口变化后的 PTY Resize
- Ctrl+C、Ctrl+D、Ctrl+L 等控制键
- 复制、粘贴和右键菜单
- 滚动缓冲和终端搜索
- 高速输出时的背压与批量刷新
- 标签关闭时正确停止读取任务并释放连接

终端输出不得无限追加到 WPF TextBox，也不得把全部历史日志长期保存在内存中。

## 3.3 SFTP 文件管理

基础能力：

- 查看目录和文件列表
- 进入目录、返回上级、刷新
- 上传文件
- 下载文件
- 拖拽上传
- 新建目录
- 重命名
- 删除文件或目录
- 查看文件大小、权限、修改时间

传输体验：

- 显示文件名、总大小、已完成大小、速度和进度
- 支持取消
- 上传/下载冲突时询问覆盖、跳过或重命名
- 失败后给出明确原因
- 传输过程不阻塞 UI
- 大文件使用流式传输，禁止一次性读入内存

1.0 版本不强制实现断点续传；先确保普通传输稳定，再在后续版本加入。

## 3.4 日志快捷功能

每台服务器可以配置多个日志入口：

```text
名称：应用实时日志
路径：/opt/app/logs/application.log
命令：tail -n 500 -F {path}
```

推荐功能：

- 服务器右键直接打开常用日志
- 一键执行 `tail -n 500 -F`
- 一键进入日志所在目录
- 一键下载当前日志
- 常用命令收藏
- 命令执行前允许用户预览

注意事项：

- 路径和关键词必须进行 Shell 参数转义。
- 不直接拼接未经处理的用户输入，避免命令注入。
- 1.0 不做完整日志索引、搜索引擎和图表分析。

## 3.5 会话管理

每个标签页包含独立状态：

```text
服务器配置快照
SSH 连接
ShellStream
终端 WebView2 实例
SFTP 连接或延迟创建工厂
CancellationTokenSource
连接状态
最后错误
```

状态机建议：

```text
Disconnected
    ↓
Connecting
    ↓
Connected
    ↓
Reconnecting / Disconnecting
    ↓
Disconnected / Failed
```

所有连接和断开操作应具备幂等性，避免用户连续点击导致重复连接或重复释放。

---

## 4. 数据模型

建议的核心表：

### server_group

```text
id
name
sort_order
created_at
updated_at
```

### server_profile

```text
id
group_id
name
host
port
username
auth_type
password_cipher
private_key_path
private_key_passphrase_cipher
default_remote_path
connect_timeout_seconds
keep_alive_seconds
host_key_fingerprint
remark
last_connected_at
created_at
updated_at
```

### log_profile

```text
id
server_id
name
remote_path
command_template
sort_order
```

### quick_command

```text
id
name
command
scope
server_id
sort_order
```

### app_setting

```text
key
value
```

### recent_session

```text
id
server_id
opened_at
closed_at
close_reason
```

传输历史可以后续加入，MVP 阶段不必先建复杂任务表。

---

## 5. 版本与里程碑

以下按单人兼职开发、工作日晚间加周末投入估算，总周期约 8～12 周。进度应以验收标准为准，而不是机械追求日期。

## 阶段 0：技术验证与项目骨架

**目标：** 最小链路跑通，不做完整 UI。

任务：

- 创建解决方案和分层项目
- 接入 SSH.NET
- 使用测试服务器建立 SSH 连接
- 创建交互式 Shell/PTY
- WPF 中嵌入 WebView2
- 加载本地 xterm.js 页面
- 打通 xterm.js → C# → ShellStream 输入
- 打通 ShellStream → C# → xterm.js 输出
- 实现窗口 Resize 通知
- 建立基础日志和异常处理

验收：

- 能连接一台 Linux 测试机
- 能执行 `ls`、`cd`、`pwd`
- `tail -f` 能持续输出
- `vim` 或 `top` 的基础显示和退出正常
- 调整窗口大小后终端行列数同步变化

## V0.1：可用 SSH 终端

任务：

- 新建连接窗口
- 密码登录
- 私钥登录
- 连接中、已连接、失败、已断开状态提示
- 单标签终端
- 复制、粘贴、清屏、终端搜索
- 关闭标签时释放连接
- 手动断开和重连

验收：

- 连续连接、断开 20 次无明显资源泄漏
- `tail -F` 持续运行时界面不卡顿
- 中文命令和中文输出正常
- 错误密码、超时、服务器拒绝连接时提示明确

## V0.2：服务器管理

任务：

- SQLite 初始化和迁移机制
- 服务器 CRUD
- 服务器分组
- 搜索和最近连接
- DPAPI 加密凭据
- 主机指纹首次确认和后续校验
- 双击服务器打开标签页

验收：

- 重启应用后配置保留
- 数据库中无法直接看到明文密码
- 主机指纹变化时阻止静默连接并明确告警
- 同一服务器可以快速再次连接

## V0.3：SFTP MVP

任务：

- 远程目录列表
- 路径导航、返回上级、刷新
- 上传和下载
- 进度显示和取消
- 覆盖冲突处理
- 新建目录、重命名和删除
- Windows 文件拖拽上传

验收：

- 上传和下载常见小文件正常
- 传输 1GB 级文件时内存不随文件大小线性增长
- 取消传输后连接仍可继续使用，或能自动恢复
- 中文文件名和目录名可正常处理
- 权限不足、磁盘空间不足、连接断开时提示明确

## V0.4：多标签与日常体验

任务：

- 多 SSH 标签页
- 标签关闭确认和快捷键
- 标签标题显示服务器名及连接状态
- SFTP 面板按标签绑定服务器
- 会话状态隔离
- 最近服务器和快速搜索
- 深色、浅色主题（已实现并通过即时应用、子窗口同步及重启恢复验收）
- 字体、字号、滚动行数设置（已实现；完整界面输入与重启恢复待验收）

验收：

- 同时打开 5～10 个终端标签仍可正常操作
- 某个会话断开不会影响其他标签
- 关闭应用时可快速、可靠释放全部会话

## V0.5：日志快捷能力

测试依据：`LiteTerm_V0.5_测试计划.md`。常用命令和按服务器日志入口的最小纵向链路已实现；命令配置、模板转义、预览、标签发送和 LOG 用例进入验收，一键下载用例在对应功能完成后启用。

任务：

- 常用日志配置（已完成最小纵向版本：按已保存服务器隔离、SQLite 版本 4 持久化、CRUD）
- 常用命令配置（已完成最小纵向版本：新增、编辑、删除、SQLite 持久化）
- 一键 `tail -n 500 -F`（已实现安全预览和当前标签明确执行）
- 一键进入日志目录（已实现安全预览和当前标签明确执行）
- 一键下载日志
- 命令执行预览（已实现 `{path}` 变量、POSIX 参数转义和执行前预览）
- 日志关键词基础高亮可作为可选增强

验收：

- 从服务器列表到打开目标日志不超过少量操作
- 带空格或特殊字符的路径经过正确转义
- 非法命令模板不会直接导致程序崩溃

## V0.6：稳定性与安全加固

任务：

- KeepAlive
- 网络断开检测
- 手动重连流程
- 可选自动重连
- SSH/SFTP 超时和取消
- 统一异常映射
- WebView2 初始化失败处理
- 应用诊断日志
- 敏感字段脱敏
- 并发与资源释放排查

验收：

- 断网后状态可以正确变化
- 恢复网络后能手动重连
- 远端主动断开时不会卡死
- 长时间运行无持续 CPU 异常和明显内存增长
- 诊断日志中不出现密码和私钥口令

## V0.9：发布候选版

任务：

- 应用图标和版本信息
- 首次启动引导
- 配置数据库升级测试
- WebView2 Runtime 检测和安装提示
- 自包含发布
- 安装包或便携压缩包
- 崩溃恢复和配置备份
- 用户手册和快捷键说明

验收：

- 在一台干净 Windows 机器上安装并运行
- 没有开发环境也可正常连接服务器
- 升级版本不会丢失已有服务器配置
- 卸载不会误删用户主动保留的数据

## V1.0：稳定自用版

发布标准：

- SSH、终端、SFTP、服务器管理均达到日常可用
- 不存在阻断核心流程的已知缺陷
- 凭据没有明文保存
- 主机指纹校验可用
- 支持连续数小时查看日志
- 支持大文件流式传输
- 提供基本使用说明和错误排查说明

---

## 6. 详细开发任务拆分

## 6.1 第一周建议

### 第 1～2 天：项目搭建

- 创建 Git 仓库
- 创建解决方案和项目分层
- 配置依赖注入
- 配置日志
- 建立统一结果和异常模型
- 创建 MainWindow、ServerList、TerminalHost 占位界面

### 第 3～4 天：SSH 连接验证

- 编写 `ISshConnectionFactory`
- 实现密码认证
- 实现私钥认证
- 创建 ShellStream
- 编写异步读取循环
- 处理取消和释放

### 第 5～7 天：终端桥接

- 加载 xterm.js
- 实现 WebView2 消息通信
- 输入写入 ShellStream
- 输出批量写入 xterm.js
- 集成终端尺寸自适应
- 测试 `tail -f`、`vim`、`top`

第一周结束时必须得到一个“丑但能用”的 SSH 终端，不要提前投入服务器树、主题和复杂动画。

## 6.2 第二周建议

- 优化终端输入输出队列
- 增加连接状态
- 增加错误提示
- 支持复制、粘贴和搜索
- 完成连接页
- 处理关闭标签、断开连接和程序退出
- 编写第一批集成测试

## 6.3 第三周建议

- SQLite 数据结构
- 服务器 CRUD
- 分组与搜索
- DPAPI 凭据服务
- 主机指纹确认
- 最近连接

## 6.4 第四至五周建议

- SFTP 目录列表
- 上传下载
- 进度与取消
- 文件操作
- 拖拽上传
- 大文件测试

后续按版本里程碑继续推进。

---

## 7. 测试计划

## 7.1 单元测试

重点覆盖：

- 配置校验
- Shell 参数转义
- 路径拼接
- 凭据加密和解密
- 主机指纹比较
- 会话状态机
- 重连策略
- 文件冲突策略
- 数据库 Repository

## 7.2 集成测试

建议准备一台专用 Linux 虚拟机、WSL 或容器化 OpenSSH 测试环境，覆盖：

- 密码认证
- RSA/Ed25519 私钥认证
- 错误密码
- 错误私钥
- 主机指纹首次连接和变化
- SSH 超时
- 服务器主动断开
- 网络瞬断
- SFTP 权限不足
- 远端磁盘空间不足
- 大文件传输
- 中文文件名

## 7.3 终端兼容性测试

至少测试：

```bash
ls --color=auto
cat
less
vim
top
htop
tail -n 500 -F application.log
grep --color=always ERROR application.log
tmux
```

检查内容：

- 颜色
- 光标
- 退格与删除
- 方向键
- Ctrl 组合键
- 中文宽字符
- 窗口缩放
- 大量高速输出

## 7.4 发布测试矩阵

Windows：

- Windows 10 x64
- Windows 11 x64
- 不同 DPI 缩放比例
- 有/无预装 WebView2 Runtime 的环境

远程服务器：

- Ubuntu/Debian 系
- CentOS/RHEL/AlmaLinux 系
- OpenSSH 不同常见版本

---

## 8. 安全设计

### 8.1 凭据

- 密码和私钥口令使用当前 Windows 用户上下文进行 DPAPI 加密。
- 应用日志不得记录密码、私钥内容或完整认证异常对象。
- UI 中密码默认遮挡，不提供无必要的明文查看。
- 复制密码到剪贴板时应给出明确提示，可考虑延迟清理剪贴板。

### 8.2 主机身份

- 首次连接显示主机指纹并要求确认。
- 确认后保存指纹。
- 后续指纹不一致时默认阻止连接。
- 不提供默认“忽略所有主机验证”的全局开关。

### 8.3 WebView2 与本地页面

- 终端页面使用应用内本地静态资源，不加载远程网页。
- 限制 WebView2 导航到外部地址。
- C# 与 JavaScript 消息定义固定协议并验证数据结构。
- 不允许终端输出直接注入 HTML。

### 8.4 命令模板

- 路径、关键词等变量必须转义。
- 执行前展示最终命令。
- 常用命令由用户显式保存，不从远程输出自动生成并执行。

---

## 9. 性能与稳定性指标

建议设定以下目标：

- 冷启动尽量控制在数秒内
- 空闲状态 CPU 接近 0
- 单个空闲会话不持续轮询占用 CPU
- 打开 10 个会话仍可正常切换
- 高速日志输出时 UI 保持可交互
- 输出采用缓冲合并，避免每个字节或每行都跨 WebView2 调用
- SFTP 大文件采用流式传输
- 会话关闭后线程、Task、连接和 WebView2 事件均被释放
- 程序退出不因等待网络线程而长时间卡住

---

## 10. 主要风险与应对

| 风险 | 表现 | 应对策略 |
|---|---|---|
| 终端输出过快 | UI 卡顿、内存上涨 | 输出队列、批量刷新、背压、限制 scrollback |
| PTY 尺寸不同步 | `vim/top` 显示错乱 | xterm Fit 后将行列数同步到 SSH PTY |
| 会话释放不彻底 | 关闭后线程仍运行 | 统一 Session 生命周期、CancellationToken、幂等 Dispose |
| WebView2 初始化失败 | 终端白屏 | Runtime 检测、错误页、重新初始化 |
| 凭据泄露 | SQLite 或日志出现密码 | DPAPI、脱敏、严格日志策略 |
| 主机被替换 | 中间人风险 | 首次确认并固定主机指纹 |
| 大文件传输占内存 | OOM 或卡顿 | 流式传输、进度回调、取消机制 |
| 命令拼接注入 | 误执行危险命令 | 参数转义、命令预览、限制模板变量 |
| 功能不断膨胀 | 项目延期 | 严格维持 1.0 范围，不加入 RDP/X11 等功能 |

---

## 11. 开发规范

### 11.1 代码规范

- UI 不直接操作 SSH.NET 对象。
- ViewModel 不包含具体数据库和网络实现。
- 所有长耗时操作使用异步 API。
- 所有异步操作支持取消或明确不可取消。
- 不使用 `async void`，事件处理器除外。
- 不在 UI 线程进行阻塞读取、文件传输或数据库大操作。
- 连接、流和 CancellationTokenSource 必须明确释放。

### 11.2 Git 规范

建议分支：

```text
main          稳定版本
develop       日常集成
feature/*     功能分支
fix/*         缺陷修复
release/*     发布准备
```

提交示例：

```text
feat(terminal): add WebView2 xterm input bridge
feat(sftp): add upload progress and cancellation
fix(session): stop read loop when tab is closed
refactor(data): separate server repository interface
```

### 11.3 Issue 模板

每个任务至少包含：

- 背景
- 目标
- 非目标
- 实现方案
- 验收条件
- 测试方法

---

## 12. 完成定义

一个功能只有同时满足以下条件才算完成：

- 功能代码已实现
- 错误路径已处理
- 不阻塞 UI
- 资源可以释放
- 有必要的日志
- 核心逻辑有测试
- 在真实 Linux 服务器验证
- 不泄露敏感数据
- 文档或快捷键说明已更新

---

## 13. 后续版本候选

1.0 稳定后再评估：

- SFTP 断点续传
- 双栏本地/远程文件管理器
- SSH 跳板机/ProxyJump
- SOCKS5 和端口转发
- 会话恢复
- 命令历史同步
- 日志关键词客户端高亮
- 文件差异对比
- 自动更新
- 配置加密导入导出

这些功能不得影响 1.0 的交付。

---

## 14. 最先执行的任务

第一项任务不是画完整界面，而是完成一条可验证的纵向链路：

```text
新建测试连接
  → SSH.NET 建立连接
  → 创建 ShellStream/PTY
  → xterm.js 输入命令
  → Linux 执行命令
  → xterm.js 显示结果
  → Resize 正常
  → 关闭窗口后连接和读取任务全部释放
```

这条链路一旦稳定，服务器管理、SFTP 和日志快捷功能都可以在其上逐步扩展；如果这条链路不稳定，提前开发复杂 UI 没有意义。
